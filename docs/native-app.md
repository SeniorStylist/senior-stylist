# Native App (iOS + Android) — Capacitor Wrapper

Senior Stylist ships to the App Store and Google Play as a **Capacitor** native shell that loads the
live Vercel site (`https://portal.seniorstylist.com`) in a managed WebView. The backend (middleware
auth, SSR, ~180 API routes, crons) is unchanged — the native projects are just packaging + native
plugin access.

Full strategy, sequencing (Android first), and the Apple Guideline 4.2 plan live in
`/root/.claude/plans/command-name-clear-command-name-command-swirling-cascade.md`.

## Layout

- `capacitor.config.ts` — appId `com.seniorstylist.app`, WebView points at the live site
  (override with `CAP_SERVER_URL` for staging), brand splash/status-bar/push config.
- `android/` — generated Android Studio project (committed; build artifacts gitignored).
- `ios/` — generated Xcode project (committed; Pods/build gitignored). **Requires macOS to build.**
- `assets/` — source brand images for `@capacitor/assets` (icon + splash), generated by
  `scripts/generate-native-assets.mjs` from `public/seniorstylistlogo-white.png`.
- `src/lib/detect-device.ts` — `isNativeApp()` / `nativePlatform()` distinguish the native shell
  from browser/PWA so UI can branch to native plugins.

## npm scripts

| Script | What it does |
|---|---|
| `npm run cap:assets` | Regenerate source brand images, then fan them into native icon/splash sets. |
| `npm run cap:sync` | Copy config + plugins into both native projects (run after dep/plugin changes). |
| `npm run cap:android` | Sync + open the project in Android Studio. |
| `npm run cap:ios` | Sync + open the project in Xcode (macOS only). |

> `@capacitor/assets` and the iOS toolchain don't install in the CI/web container (native sharp +
> CocoaPods). Run `cap:assets` and all iOS steps on a local dev/build machine.

## Build & submit (summary)

**Android (ship first):**
1. `npm run cap:assets` (once, or when branding changes).
2. `npx cap sync android`.
3. In Android Studio (or `cd android && ./gradlew bundleRelease`) produce a signed `.aab`.
4. Play Console → closed test (14 days, 12+ testers) → production.

**iOS (after Android):**
1. `npm run cap:sync` then open in Xcode (`npm run cap:ios`).
2. `pod install` runs on first sync (macOS).
3. Archive → upload to App Store Connect → TestFlight → submit for review.
4. In review notes, list the native features (push, biometrics, native camera) and provide a demo
   login to pre-empt Guideline 4.2.

## Open follow-ups (see plan Phase C)

- Native push (FCM/APNs) bridge + `push_subscriptions.platform` column.
- Biometric unlock, native camera for OCR/check-scan, deep-link/auth-return handling.
- **Validate Supabase cookie auth inside the WebView on a real device before further investment.**
