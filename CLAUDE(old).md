# Senior Stylist — Claude Code Rules

## Project
- Live site: https://senior-stylist.vercel.app
- GitHub: https://github.com/SeniorStylist/senior-stylist
- Supabase project: goomnlsdguetfgwjpwer

## Non-Negotiable Rules

### Database
- ALWAYS scope every DB query to facilityId — never return data across facilities
- NEVER hard delete records — always use active=false
- Prices are ALWAYS stored in cents (integers) in the DB
- Display prices by dividing by 100, NEVER store floats
- After ANY schema change run: npx dotenv -e .env.local -- npx drizzle-kit push
- facilityUsers.role is the authoritative role — not profiles.role
- facilities.working_hours is jsonb `{ days: string[], startTime: "HH:MM", endTime: "HH:MM" }` — null = default 08:00–18:00; use it to bound time slots in booking modal
- ALL tables MUST have RLS enabled with a `service_role_all` policy — when adding a new table, run `ALTER TABLE x ENABLE ROW LEVEL SECURITY` and `CREATE POLICY "service_role_all" ON x FOR ALL TO service_role USING (true) WITH CHECK (true)`
- NEVER disable RLS on any table — all DB access goes through service_role (Drizzle + SUPABASE_SERVICE_ROLE_KEY); the anon key is auth-only and must never have direct table access
- MIDDLEWARE RLS EXCEPTION: `facility_users` and `invites` have an additional scoped `authenticated` SELECT policy so that middleware (which uses the anon key / `authenticated` role) can query them. `facility_users` → `user_id = auth.uid()`; `invites` → `email = auth.jwt()->>'email'`. Without these, middleware queries silently return empty and every user gets redirected to /unauthorized. If you add a new table that middleware needs to query, add a scoped `authenticated` SELECT policy for it.

### Git
- ALWAYS use git add -A (project path has parentheses that break zsh globs)
- NEVER commit .env.local
- Always run npx tsc --noEmit before committing — fix all errors first

### Auth & Roles
- role 'admin' — full access to everything
- role 'stylist' — calendar, log, own earnings only. No reports, no settings
- role 'viewer' — read-only, no edits
- role 'super_admin' — franchise owner; scoped to only the facilities in their franchise (via franchises + franchise_facilities tables). Facility switcher in layout.tsx filters to franchise facilities only. Assigned via facility_users.role = 'super_admin' by master admin. **`getUserFacility()` normalizes 'super_admin' → 'admin' at read time** so all page guards (`role !== 'admin'`), API guards, and nav item filters work correctly for franchise owners without touching every call site. The normalization is in `src/lib/get-facility-id.ts` (`normalizeRole()` helper). layout.tsx also normalizes `activeRole` independently since it queries `facilityUsers` directly. The Super Admin page and link remain gated by `NEXT_PUBLIC_SUPER_ADMIN_EMAIL` email match (not role) — normalizing the role does not grant or remove `/super-admin` access.
- Super admin (master admin) bypasses all checks via NEXT_PUBLIC_SUPER_ADMIN_EMAIL env var
- Franchise system: `franchises` table (id, name, owner_user_id → profiles). `franchise_facilities` join table (franchise_id, facility_id, CASCADE on both). When a franchise is created/updated, facilityUsers rows are upserted for the owner with role='super_admin' on all franchise facilities. API: GET/POST /api/super-admin/franchises, PUT/DELETE /api/super-admin/franchises/[id]. CRUD UI in /super-admin page Franchises section.
- Portal routes (/portal/*) are PUBLIC — token = auth, no login required
- Invoice routes (/invoice/*) are PUBLIC — printable pages

### API Routes
- Every API route must check auth via Supabase createClient().auth.getUser()
- Every API route must scope to facilityId via getUserFacility()
- Return { data: ... } on success, { error: "message" } on failure
- Always wrap DB queries in try/catch — never let a DB error crash a page
- Mutation routes (POST/PUT/DELETE) on shared-state resources (stylists, services, OCR/parse-pdf) MUST add `if (facilityUser.role !== 'admin') return Response.json({ error: 'Forbidden' }, { status: 403 })`. Stylist-role users can only mutate their own bookings/profile.
- Every Zod input schema needs `.max()` caps: names 200, room 50, notes/description 2000, email 320, color 20, address 500, cents 10_000_000, tier/option arrays 20, additionalServices 20, timezone 100. Without caps, attackers can waste DB space / bandwidth.
- Rate-limit all public or expensive routes via `src/lib/rate-limit.ts` — `checkRateLimit(bucket, identifier)` + `rateLimitResponse(retryAfter)`. Buckets: `signup` 5/hr/IP, `portalBook` 10/hr/token, `ocr` 20/hr/user, `parsePdf` 20/hr/user, `sendPortalLink` 10/hr/user, `invites` 30/hr/user. No-ops when UPSTASH_REDIS_REST_URL/TOKEN are unset — set them in Vercel for production protection.
- Portal routes (`/api/portal/[token]/*`) MUST use explicit `columns:` whitelists on every `db.query.*` call, and MUST verify every `stylistId`/`serviceId`/`addonServiceIds` belongs to the resident's facility before accepting input. Never trust client-supplied IDs.

### Security / Payload Hygiene
- **Server → client payloads MUST be sanitized.** Use `src/lib/sanitize.ts` helpers at every boundary:
  - `sanitizeStylist(row)` — drops `googleRefreshToken`
  - `sanitizeFacility(row)` — drops `stripeSecretKey`, adds `hasStripeSecret: boolean`
  - `toClientJson(value)` — recursive replacer that strips sensitive keys anywhere in nested shapes. Use in place of `JSON.parse(JSON.stringify(x))` for anything with stylists/facilities embedded.
- Stripe secret is write-only in the Settings UI — server never sends it; client shows a masked placeholder + "Stored securely — enter a new key to replace" when `hasStripeSecret`.
- **OAuth CSRF pattern** (Google Calendar connect/callback): `oauth_states` table stores a nonce bound to `user_id` + `stylist_id`. `/api/auth/google-calendar/connect` requires admin, generates `crypto.randomUUID()` nonce, inserts row, passes `state=nonce` to Google. `/api/auth/google-calendar/callback` requires the same authenticated user, looks up the row, enforces 10-min TTL, verifies stylist still in caller's facility, deletes row atomically after use. Never trust raw `Buffer.from(state,'base64')` as a stylist id.
- `next.config.ts` sets response headers: X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy strict-origin-when-cross-origin, Permissions-Policy (camera/mic/geo all off — file-picker `capture="environment"` works without it), HSTS 1yr, Content-Security-Policy (default-src 'self', allowlists for Supabase/Google/Upstash/Vercel Insights, unsafe-inline styles for Tailwind, frame-ancestors 'none').

### Database Connection
- Use the pooler URL (port 5432, session mode) for DATABASE_URL
- max: 1 connection, idle_timeout: 20, connect_timeout: 30
- NEVER set max > 1 in session mode — Supabase session pooler holds a real Postgres connection per serverless instance; multiple instances with max > 1 quickly exhaust the connection limit and throw `MaxClientsInSessionMode`
- The client is a singleton via globalThis._pgClient
- When a route runs 3+ sequential DB queries, wrap them in db.transaction() to share a single connection

### UI / Design
- Design system: stone colors, rounded-2xl cards
- **Brand palette (from seniorstylist.com marketing site):** primary `#8B2E4A` (burgundy), hover `#72253C`, portal bg `#FDF8F8` (warm blush), accent `#C4687A` (rose)
- **Entire app uses BURGUNDY palette** — `#8B2E4A` primary, `#72253C` hover, `#C4687A` accent. `bg-rose-50` for tinted backgrounds, `border-rose-100/200` for borders, `focus:ring-rose-100` for focus rings, `accent-[#8B2E4A]` for checkboxes. Exception: `completed` status badges remain `bg-teal-50 text-teal-700` (semantic color, intentional).
- **Admin app now uses burgundy `#8B2E4A`** everywhere — full brand migration complete. All components (`button.tsx`, `input.tsx`, calendar, booking modal, sidebar active states, panels, UI primitives, email templates) use `#8B2E4A`. Do NOT add new teal `#0D7377` anywhere in the app.
- **Sidebar background** is `--color-sidebar: #1C0A12` (very dark burgundy) — defined in `globals.css`, used via `style={{ backgroundColor: 'var(--color-sidebar)' }}` in `sidebar.tsx`. Do NOT use `#0D2B2E` anywhere.
- **Logo file:** `/public/Seniorstylistlogo.jpg` — use `<Image>` from `next/image`. Three placement strategies: (1) **Sidebar** — wrap in `<Link href="/dashboard">`, apply `style={{ filter: 'brightness(0) invert(1)' }}` so logo renders white on `#1C0A12`. (2) **Portal header** — same `filter: brightness(0) invert(1)` to render white on `#8B2E4A`, wrapped in `<a href="https://seniorstylist.com" target="_blank">`. (3) **White-background pages** (login, invite-accept, unauthorized) — show naturally, no filter or wrapper.
- Portal layout header: `style={{ backgroundColor: '#8B2E4A' }}` with inline floral SVG rose accent (`rgba(255,255,255,0.15)` stroke, `80×96px`, positioned absolute top-right)
- `--color-primary` CSS variable in globals.css is `#8B2E4A` — affects FullCalendar toolbar buttons only
- Fonts: DM Serif Display (headings), DM Sans (body)
- Bottom sheets on mobile, modals on desktop
- All bottom sheets: fixed overlay → flex-col → justify-end structure
- Footer/save buttons ALWAYS outside scroll area (flexShrink: 0)
- viewport-fit=cover required in layout.tsx for iPhone safe areas

### Common Bugs to Avoid
- NEVER use new URL(request.url).origin for redirects on Vercel — use request.nextUrl.clone()
- Long-running API routes (OCR, AI calls) MUST export `export const maxDuration = 60` and `export const dynamic = 'force-dynamic'` at the top of the file — without maxDuration, Vercel cuts the function at 10s and the client sees a network error
- NEVER put page.tsx files inside /app/api/ directories
- NEVER set selectMirror=true on FullCalendar (causes squish bug)
- NEVER make Google Calendar sync block a response — always fire-and-forget
- NEVER use position:sticky inside overflow:auto on iOS — use flexShrink:0 footer instead
- Any `fixed` element near the bottom of the screen on mobile MUST use `style={{ bottom: 'calc(env(safe-area-inset-bottom) + 80px)' }}` — the mobile nav bar is ~72px tall; adding 80px clears both it and the home indicator. Remove any Tailwind `bottom-N` class from the same element (inline style wins on specificity but keeping both is confusing). Floating action bars, FABs, scan buttons, multi-select bars all need this.
- Resident merge: POST /api/residents/merge moves bookings (residentId reassign), then renames merged resident to `${name}-merged` (BEFORE setting active=false) to free the unique(name, facility_id) constraint, then updates the kept resident's name. Always rename first, deactivate second. Never hard delete.
- Safe area insets in modals: use inline `style` (not Tailwind) for `env()` values — Tailwind cannot compute them. Header: `style={{ paddingTop: 'calc(env(safe-area-inset-top) + 20px)' }}`. Footer: `style={{ paddingBottom: 'calc(env(safe-area-inset-bottom) + 16px)' }}`. Scroll areas with bottom buttons: `style={{ paddingBottom: 'calc(env(safe-area-inset-bottom) + 80px)' }}`.
- Lightbox pattern: `const [lightboxSrc, setLightboxSrc] = useState<string | null>(null)`. Overlay: `fixed inset-0 z-[60] bg-black/90 flex items-center justify-center` with safe-area padding inline styles. Tap overlay to close. Image `onClick` stops propagation. ✕ button: `style={{ top: 'calc(env(safe-area-inset-top) + 16px)' }}` positioned `absolute right-4`.
- pdfjs-dist (v5) client-side PDF preview: `import * as pdfjsLib from 'pdfjs-dist'`; set `workerSrc` to `//cdnjs.cloudflare.com/ajax/libs/pdf.js/${pdfjsLib.version}/pdf.worker.min.js`. Turbopack (used by default in Next.js 16) handles native deps like `canvas` automatically — no webpack alias needed. Do NOT add `webpack: (config) => { config.resolve.alias.canvas = false }` — it breaks the Turbopack build. v5 `page.render()` signature: `{ canvasContext, canvas, viewport }` (must include `canvas`, not just `canvasContext`). Store rendered data URLs (JPEG 0.85) in `previews[]`; empty string `''` = still rendering → show spinner. NEVER set `workerSrc = ''` — use the CDN URL or rendering silently fails in some environments.
- **OCR upload PDF previews**: `ocr-import-modal.tsx` renders PDFs at two scales via `renderPdfPage(file, scale)`. `previews[]` (scale 0.5, thumbnail) drives the 80×80 upload grid; `fullResPreviews[]` (scale 1.5) drives the review-step "Source sheet" image and lightbox. Both are rendered in parallel in `handleFiles` for each PDF. `''` in either array = still rendering → show spinner. Image files use `URL.createObjectURL` in both arrays (same ref). Reset and removeFile both clear both arrays.
- ALWAYS call api.unselect() immediately in FullCalendar onSelect handler
- NEVER use pdf-parse or @napi-rs/canvas on Vercel — no native deps allowed
- PDF price sheet parsing (`/api/services/parse-pdf`): uses **Gemini 2.5 Flash** with the PDF sent as a native `application/pdf` inline document (base64 `inlineData`). Gemini reads the visual layout like a human — no text extraction, no regex. Returns a JSON array with `name, price, category, pricingType, addonAmountCents, pricingTiers, pricingOptions, durationMinutes`. Route maps to `ParsedService[]` (price→priceCents ×100, addon priceCents=0, pricingTiers maxQty null→999). Uses the same direct-fetch pattern as the OCR route — same endpoint, same camelCase fields, same model. Reason for switch: text extraction (both unpdf and pdfjs-dist) silently drops sections on PDFs with non-standard internal stream layouts (confirmed with Symphony Manor). Gemini vision is reliable regardless of PDF structure.
- Addon services multi-select: booking modal shows a checklist of addon-type services (pricingType === 'addon', excluding primary service) below the primary service selector. State: `selectedAddonServiceIds: string[]`. Price breakdown shows when ≥1 addon is checked. Payload includes `addonServiceIds: string[]`. POST /api/bookings fetches those services (inArray guard required), sums addon_amount_cents → multiAddonTotalCents, adds to resolved primary price. Stores addon_service_ids and final addon_total_cents on booking. Same pattern in log walk-in form.
- Addon price fallback in booking modal: addon-type services display their surcharge as `(svc.addonAmountCents ?? svc.priceCents ?? 0)` — NOT just `addonAmountCents`. Services created manually may store the surcharge in `priceCents` with `addonAmountCents = null`. Apply this fallback at ALL three sites: `multiAddonTotal` reduce, checklist label span, and breakdown line item span.
- `formatPricingLabel()` for addon type: returns `+$X.00` (e.g. `+$15.00`) — NOT `$0.00 + $15.00`. Uses `(addonAmountCents ?? priceCents)` as the surcharge value. This is the canonical display format used in both the services list and the booking modal selector.
- `services` table has a `category` text column (nullable). Populated by bulk import from PDF parser. Shown as muted sub-label under service name in the services list. Passed through the entire import pipeline: parse-pdf → ParsedService.category → POST body → bulk Zod schema → Drizzle insert.
- Service option text in booking modal: `\`${s.name} · ${formatPricingLabel(s)}\`` — no duration suffix, no dash. Clean for mobile.
- **Single booking UI for all platforms.** `booking-modal.tsx` uses `useIsMobile()` to switch between `<Modal>` (desktop) and `<BottomSheet>` (mobile); both share the exact same form body. **Never build a parallel mobile booking flow.** `QuickBookFAB` (`src/components/calendar/quick-book-fab.tsx`) is a pure ~35-line button — `md:hidden`, fixed `bottom: calc(env(safe-area-inset-bottom) + 80px)`, `z-40`, single `onOpen: () => void` prop. Dashboard wires it to `openQuickCreate()` which picks the next 30-min slot from now and calls `openCreateModal(start, end)`. Calendar slot-select (`onSelectSlot` → `openCreateModal`) also opens booking-modal — do NOT reintroduce the mobile special-case that routed to a separate flow. Any pricing-UI fix to booking-modal automatically reaches mobile.
- **BottomSheet structural invariants** (`src/components/ui/bottom-sheet.tsx`): the drag handle (44px, flexShrink:0), optional title header (flexShrink:0), scroll area (flex:1, overflowY:auto, minHeight:0), and footer (flexShrink:0) are FLEX SIBLINGS inside the sheet panel — NOT overlay / fixed / sticky children on top of the scroll area. The first child of the scroll area renders immediately below the header. Never wrap booking-modal content in its own sticky/fixed header; never assume the BottomSheet's title header overlays content.
- **booking-modal scroll-to-top** uses a dedicated `useLayoutEffect` with deps `[open, isMobile]` (not part of the form-prefill `useEffect`). Both deps are required: `open` triggers the reset on each re-open, and `isMobile` re-runs after `useIsMobile()` flips so the DOM walk hits the BottomSheet's scroll container (the initial Modal render has no scroll ancestor). The walk uses `window.getComputedStyle(el).overflowY` === `auto`/`scroll` to find the ancestor.
- **`useIsMobile()` is lazy-initialized from `window.innerWidth`** on client (falls back to `false` when `typeof window === 'undefined'`). This avoids a first-render desktop→mobile flash in conditional renderers like BookingModal that pick Modal vs BottomSheet on the first paint. Do NOT revert to `useState(false)` initial value.
- **Category grouping in service pickers:** booking-modal primary service `<select>` and addon checklist group by `service.category` via a local `groupByCategory()` helper returning `Array<[string, T[]]>`. Key is `category?.trim() || 'Other'`. Sort order: follows per-facility `serviceCategoryOrder` when present, falls back to **Z→A descending** when absent; "Other" always last. When `groups.length <= 1`, skip the grouping wrapper and render flat — avoids a lone `<optgroup>` or section header when every service shares one category. The `<select>` uses `<optgroup label={category}>`; the addon checklist uses a text sub-header `text-xs font-medium text-stone-500 uppercase tracking-wide`. **Within each category group**, services are sorted by `pricingTypePriority`: fixed/multi_option first (0), tiered second (1), addon last (2); then alphabetically by name within each tier. `addonServices` derived constant is also sorted the same way at definition.
- **Per-facility service category order** (`facilities.service_category_order jsonb`): nullable `string[]` captured on PDF import in `src/app/api/services/bulk/route.ts`. After the insert, the route extracts unique categories from the uploaded rows in order of first appearance (skips empty + "Other"), merges with the existing order (existing entries keep their positions, new ones appended), and writes back. Merge+write wrapped in try/catch that logs but does not fail the response. Applied everywhere services render: booking modal (create + edit), portal service picker, services page (only when `sortKey === 'category'`), log walk-in form `<select>`. All four client components accept an optional `serviceCategoryOrder?: string[] | null` prop — dashboard threads it via `facility.serviceCategoryOrder` → `<BookingModal>`; log/services/portal pages fetch it alongside their existing queries and pass directly. `null` = fall back to Z→A alphabetical (legacy behavior). Helpers in `src/lib/service-sort.ts`: `buildCategoryPriority`, `sortCategoryGroups`, `sortServicesWithinCategory`.
- **Residents page list:** `residents-page-client.tsx` has sortable columns: `sortKey` ('name' | 'room' | 'lastVisit' | 'totalSpent') + `sortDir` ('asc' | 'desc'). Room uses `localeCompare({ numeric: true })`; nulls sort last for asc, first for desc on lastVisit/room. Same sortable-button pattern as services page (teal + ↑↓ arrow when active). `cn` import required.
- **Invite default facility:** `settings-client.tsx` initializes `inviteFacilityId` with `facility.id` (not `''`) so the current facility is pre-selected. The `loadFacilitiesForInvite` fallback to `list[0].id` remains as a guard but is normally never needed.
- **Admin booking modal never auto-selects a service.** `selectedServiceIds` initializes to `[]`, stays `[]` on modal open in create mode, and stays `[]` on resident change — no `mostUsedServiceId` useEffect in `booking-modal.tsx`. Also: `addAnotherService` appends `''` (empty slot), NOT the first primary candidate's ID — the dropdown's `<option value="">Select a service</option>` placeholder renders for empty slots, forcing the admin to pick manually. Never push `firstFree.id` or any other auto-picked service ID into `selectedServiceIds`. `handleSubmit` guards against partially-filled rows via `selectedServiceIds.some((id) => !id)` — any blank slot blocks submit with "Please fill in all required fields." The prior auto-select behavior was confusing (users saw a resident's last-used service or an alphabetically-first service pre-filled and thought they were editing an existing booking). `mostUsedServiceId` still lives on the `Resident` type and is still computed via `getMostUsedServiceIds()` because the **portal** uses it (`portal-client.tsx` `startBooking()` pre-selects the resident's top service) — only the admin-side paths were removed. Walk-in form in `log-client.tsx`: `wiServiceId` initial state is `''`, reset after submit is `''`, and the `mostUsedServiceId` useEffect that auto-set `wiServiceId` on resident change has been fully removed — never `services[0]?.id`, never `resident.mostUsedServiceId`.
- **Portal multi-service booking** (`src/app/(resident)/portal/[token]/portal-client.tsx`): fully implemented. State: `selectedServiceIds: string[]` array. UI: card-based picker grouped by category; after first selection shows collapsed summary row + "+ Add another service" dashed button (capped at 4 total slots, `min-h-[56px]` touch target); addon checklist below primary; live price breakdown. API: `handleBook` sends `serviceIds: selectedServiceIds`; `POST /api/portal/[token]/book` accepts `serviceIds[]` + `addonServiceIds[]` + `selectedQuantity` + `selectedOption`; server resolves all prices, stores `serviceIds`, `addonServiceIds`, `addonTotalCents`. **Portal pre-selection**: `portal/[token]/page.tsx` queries non-cancelled bookings for the resident grouped by serviceId, finds `mostUsedServiceId`, passes as prop; `startBooking()` pre-selects it if the service still exists and is non-addon. New residents with no history → `selectedServiceIds: []` (full picker shown empty).
- **Inline create resident in comboboxes** (booking-modal.tsx + log-client.tsx walk-in form): when ≥3 chars are typed and no residents match, a "+ Create 'name'" button appears. Tap opens a mini-form (name pre-filled, room optional) inside the same absolute dropdown container. On confirm, POST /api/residents → push to `localNewResidents: Resident[]` state → auto-select. `localNewResidents` is merged with the `residents` prop: `const allResidents = [...residents, ...localNewResidents]` — used for both `filteredResidents` and the auto-select useEffect. The "+ Create" button also appears at the bottom of non-empty result lists (useful when partial match exists but the exact new name is needed). All create state resets on modal/form close. 409 → "A resident with this name already exists". `onMouseDown` (not `onClick`) on all dropdown buttons to beat the 150ms `onBlur` setTimeout.
- **Services page list:** `services-page-client.tsx` has sortable columns: `sortKey` ('name' | 'category' | 'duration' | 'price') + `sortDir` ('asc' | 'desc'). **Default `sortKey` is `'category'`**, **default `sortDir` is `'desc'`**. When `sortKey === 'category'`, section order follows per-facility `serviceCategoryOrder` via `sortCategoryGroups(..., buildCategoryPriority(serviceCategoryOrder))` — `sortDir === 'asc'` reverses the resulting group list. Within each category group rows are sorted by `sortServicesWithinCategory(list)`: fixed/multi_option first (0), tiered second (1), addon last (2), then alphabetical by name within each tier. When `sortKey !== 'category'`, the flat client-side sort uses `sortablePrice()` (addon → addonAmountCents ?? priceCents; tiered → first tier unitPriceCents; multi_option → first option priceCents; otherwise priceCents). Server `orderBy` remains `[desc(t.category), asc(t.name)]` as a reasonable initial-paint approximation, but the client's category-sort branch is authoritative for rendering. Category section headers are ONLY shown when `sortKey === 'category'` — all other sort modes render a flat list. Grid layout: checkbox(1) + SERVICE(3) + CATEGORY(2) + DURATION(2) + PRICE(2) + actions(2) = 12. The category sub-label under service name is REMOVED — category has its own column now; null renders as "—".
- **Modal overlay** (`src/components/ui/modal.tsx`): `items-start pt-16` (NOT `items-center`) so the modal top is always below browser chrome. Panel gets `max-h-[calc(100dvh-5rem)] overflow-y-auto` so tall content (booking form) scrolls within the panel. `sticky bottom-0` footer inside the panel sticks to the bottom of the panel's scroll container — do NOT add `overflow-y-auto` to the overlay (that would break the sticky footer).
- Tiered pricing stepper: booking modal replaces `<input type="number">` with a 44px stepper row (− stone / qty display / + teal) using IIFE in JSX so `activeTier` can be computed with local variables. Tier hint line: `{minQty}–{maxQty >= 999 ? minQty + '+' : maxQty}: $X each → $total`. Breakdown label: `ServiceName (qty × $X/ea)`. Stepper − button: `disabled={selectedQuantity <= 1}`.
- Booking modal price breakdown annotations: primary service name label is computed via IIFE at `idx === 0` — tiered adds `(qty × $X/ea)`, multi_option adds `— OptionName`, plain addon adds `(+$X add-on)`. Addon checklist lines in breakdown use `text-amber-700` (not `text-stone-500`) to visually distinguish them.
- Multi-service bookings: a single booking can include N primary services (non-addon types) + M addon services. `bookings` table has three columns: `service_ids text[]` (ordered primary IDs, first = "the primary"), `service_names text[]` (denormalized names for display, parallel to service_ids), `total_duration_minutes integer` (sum of primary service durations — addons never add duration). `serviceId` column is kept as the first of `serviceIds` for backward compat — old single-service bookings (serviceId only, serviceIds null) keep working. Pricing inputs (selectedQuantity/selectedOption/addonChecked) apply ONLY to the primary (first) service; additional primaries resolve via fixed `resolvePrice(s).priceCents`. Booking modal renders primary services as a list-with-"+ Add another service" button with trash icon per row when >1; addon section sits below a labeled "Add-ons (optional)" divider. POST/PUT/recurring bookings routes accept either `serviceId: string` or `serviceIds: string[]` via Zod `.refine((d) => d.serviceId || d.serviceIds?.length > 0)`. Calendar `eventContent()` joins `booking.serviceNames` with " + " (falls back to `booking.service?.name`); week/day view also appends addon names resolved from the services prop. Log `serviceDisplayName()` prefers `booking.serviceNames` + addon names, falls back to single service.
- OCR combined services: Gemini prompt returns `additionalServices: string[]` on each entry — when a handwritten entry contains "Shampoo + Cut", "Wash/Color", etc. (separators: `+`, `/`, `&`, `and`, `,` between service terms), first term goes in `serviceName`, rest in `additionalServices`. Review UI (`ocr-import-modal.tsx`) has parallel combo inputs per entry under an "Add-on services" section with "+ add service" / ✕ buttons; each additional service gets its own fuzzy-match "Did you mean?" banner. EntryState carries parallel arrays `additionalServices: string[]` + `additionalServiceIds: (string | null)[]`. Import handler resolves each via the existing 3-step algorithm (provided UUID → in-memory `serviceMap` → fuzzy match ≥0.8 → insert new) and inserts a single booking with `serviceIds`, `serviceNames`, `totalDurationMinutes = 30 * allServiceIds.length`.
- Log booking display: use `serviceDisplayName(booking, services)` helper to show "Primary + Addon1 + Addon2" when addonServiceIds are set. Qty shown as "(qty: N)", selected option as "— option". LogBooking interface needs addonServiceIds: string[] | null.
- Import preview (import-client.tsx): ParsedService interface includes pricingType? and addonAmountCents?. parsePDF() maps these from API response. Price column shows +$X.XX for addon services (not editable number input). Name column shows pricing type badge below the name input.
- React does NOT support indeterminate as a JSX prop — set via callback ref: ref={(el) => { if (el) el.indeterminate = ... }}
- Drizzle inArray() throws on empty array — always guard with .min(1) in Zod schema
- `revalidateTag` in Next.js 16 requires TWO arguments: `revalidateTag('tag', {})` — the second arg is a `CacheLifeConfig` object; passing an empty object `{}` satisfies the type. Single-arg form causes a TS error. Applies to all booking mutation routes.
- `unstable_cache` pattern for super-admin cross-facility reports: wrap the DB fetcher in `unstable_cache(fetcher, [cacheKey], { revalidate: 300, tags: ['bookings'] })`. Cache key must include the sorted facilityIds and the month/year so different admins/months get separate cache entries. Always call the returned function (it returns a Promise). Booking mutations call `revalidateTag('bookings', {})` to invalidate these.
- Floating action bars should use z-40 (not z-50) so they don't cover the mobile nav
- PWA icons: use `src/app/icon.tsx` + `apple-icon.tsx` with ImageResponse from `next/og` — never use @napi-rs/canvas
- Recurring bookings: self-referential FK `recurringParentId` in schema requires `(): AnyPgColumn => bookings.id` pattern
- **`handleSubmit` double-submission guard**: `setSubmitting(true)` is async state — React doesn't re-render (disable the button) until the next tick, so rapid taps or simultaneous Cmd+Enter + click slip through. Use a `submittingRef = useRef(false)` mutex alongside the `submitting` state: check `if (submittingRef.current) return` at the top of `handleSubmit`, set `submittingRef.current = true` before the fetch, and reset in `finally`. This prevents every concurrent-call scenario regardless of React batching.
- **Recurring route `revalidateTag`**: `POST /api/bookings/recurring` must call `revalidateTag('bookings', {})` before returning — it was originally missing (all other booking mutation routes have it). Without it, the super-admin unstable_cache doesn't invalidate after recurring series creation.
- date-fns is installed (`addWeeks`, `addDays`, `addMonths`) — use it for date arithmetic
- InstallBanner uses `beforeinstallprompt` (Android) + manual iOS Share instructions; checks `(display-mode: standalone)` to hide when already installed
- drizzle-kit push will interactively prompt when it detects constraints that already exist in the DB — if it hangs, apply the specific SQL directly via a one-off node script using the project's DATABASE_URL:
  ```js
  // scripts/migrate.mjs
  import postgres from 'postgres'
  const sql = postgres(process.env.DATABASE_URL, { max: 1, connect_timeout: 30, prepare: false })
  await sql`ALTER TABLE my_table ALTER COLUMN my_col DROP NOT NULL`
  await sql.end()
  ```
  Run with: `npx dotenv -e .env.local -- node scripts/migrate.mjs`. Delete the script after running.
- Route-level role guards — redirect non-permitted roles in the server page component OUTSIDE try/catch:
  - `/stylists`, `/services`, `/reports`, `/settings`: `if (facilityUser.role !== 'admin') redirect('/dashboard')`
  - `/residents`: `if (facilityUser.role === 'stylist') redirect('/dashboard')` (viewer still allowed)
  - Nav items in sidebar.tsx and mobile-nav.tsx use `roles: NavRole[]` arrays per item — filtered via `item.roles.includes(role as NavRole)`. Do NOT replace this with a STYLIST_ONLY_PATHS approach.
  - Stylist visible nav: Calendar (/dashboard), Daily Log (/log), My Account (/my-account)
- NEVER put redirect() calls inside try/catch in Next.js page components — Next.js redirect() throws a special NEXT_REDIRECT error internally, and a catch block swallows it, causing the error UI to render instead of the redirect. Always perform auth/facility checks and their redirects OUTSIDE try/catch; only wrap DB data loading in try/catch.
- Next.js 16 dynamic route params are a Promise — ALWAYS use `{ params }: { params: Promise<{ id: string }> }` and `const { id } = await params` inside the handler. The old sync pattern `{ params: { id: string } }` causes a build error.
- Super admin facility mutations: use PUT /api/super-admin/facility/[id] (not the regular /api/facility route) — it verifies NEXT_PUBLIC_SUPER_ADMIN_EMAIL and accepts `active` boolean for deactivation.
- Facility name uniqueness checks MUST include `eq(t.active, true)` — inactive/deactivated facilities should not block name reuse.
- Hard delete facilities via DELETE /api/super-admin/facility/[id] — only allowed if no bookings exist (returns 409 otherwise). Delete facility_users FK rows first, then the facility row.
- facilities.contact_email is a nullable text column — used on the /unauthorized page for the "Request Access" mailto link. Falls back to facility admin's email, then to NEXT_PUBLIC_ADMIN_EMAIL env var. Set via Settings → General → Contact Email.
- access_requests table: id, facility_id (nullable), email, full_name, status ('pending'|'approved'|'denied'), role, user_id, timestamps. POST /api/access-requests is public (no auth); facilityId is optional — new users submit with no facility. PUT /api/access-requests/[id] supports BOTH facility admin and super admin; on approve accepts facilityId + role + commissionPercent, provisions facilityUsers row + optional stylist upsert. Super admin sees all pending requests in /super-admin page and assigns facility. Settings "Requests" tab shows requests assigned to THEIR facility. Dashboard shows amber banner when count > 0.
- /unauthorized page is a 'use client' component — fetches user via createClient() from @/lib/supabase/client (NOT server). It collects name + role only; no facility picker, no fetch to /api/facilities/admin-contact. POST body: { email, fullName, userId, role } — facilityId is omitted (set to null by API). Send Request button MUST be disabled (not just silently blocked) when userEmail is null — show "Loading…" text.
- Email: use src/lib/email.ts sendEmail() for ALL transactional emails — never inline Resend fetch calls. From address is `noreply@seniorstylist.com` (domain verified via DKIM/SPF/DMARC on cPanel). Confirmed working with live emails via `send.seniorstylist.com` (mailed-by) + `seniorstylist.com` (signed-by). No further DNS work needed. NEVER await sendEmail() in a request handler — fire-and-forget only. NEXT_PUBLIC_ADMIN_EMAIL is the access request notification target. sendEmail() logs both success (messageId) and failure to Vercel logs.
- Middleware public routes (no auth required): `/login`, `/auth`, `/unauthorized`, `/invite/accept`, `/portal/*`, `/api/portal/*`, `/invoice/*`, `/api/auth/google-calendar/callback`, `/privacy`, `/terms`. Adding new public routes requires updating the `isPublic` check in src/middleware.ts.
- Middleware Cache-Control headers: authenticated requests to `/dashboard`, `/payroll`, `/settings`, `/residents`, `/stylists`, `/reports`, `/log`, `/directory` always get `Cache-Control: no-store, no-cache, must-revalidate` + `Pragma: no-cache` set on `supabaseResponse` before returning — prevents browsers/CDNs from caching PHI or payroll data. Check is `user && isSensitivePage` at the bottom of the middleware function.
- Middleware redirect preservation: when unauthenticated user hits a protected route, middleware redirects to `/login?redirect=<original path+search>`. Login page reads the `redirect` param and passes it through Google OAuth as `?next=` on the callback URL. Auth callback (`/auth/callback`) reads `?next=` and redirects there instead of always `/dashboard`. This preserves the user's destination through the entire OAuth flow.
- Middleware invite redirect: when an authenticated user has no facilityUser and has a valid pending invite (matched by email), middleware redirects them to `/invite/accept?token=X` — do NOT let them roam protected routes without a facilityUser. The invite query selects `id, token`. If no invite, redirect to `/unauthorized`.
- Middleware /onboarding + /invite bypass: authenticated users with no facilityUser are always allowed through to `/onboarding` and `/invite` paths regardless of invite status.
- Onboarding wizard is 6 steps (type Step = 1 | 2 | 3 | 4 | 5 | 6); progress = (step / 6) * 100. Step 4 = Services (choose/manual/import), Step 5 = Residents (choose/manual/import), Step 6 = Done summary.
- CSV/Excel import in onboarding: use papaparse (CSV) and xlsx (Excel) client-side; detect columns by normalizing headers toLower().replace(/\s+/g,''); snap duration to nearest [15,30,45,60,75,90,120].
- papaparse is already installed (`Papa from 'papaparse'`); xlsx is already installed (`* as XLSX from 'xlsx'`).
- Onboarding Step 5 (Residents) uses /api/residents/bulk (not /api/residents) for CSV/Excel import — POST `{ rows: [{ name, roomNumber? }] }`, returns `{ data: { created: N } }`.
- Stylist dashboard mobile view: when role === 'stylist' && isMobile, show today's appointment list (not FullCalendar). setStylistListMode(true) in useEffect. Uses existing fetchBookings.
- Invite accept page does NOT check email — the token is the proof. Any authenticated user with a valid, unexpired, unused token is accepted. Remove/never add an email match guard.
- Invite accept page is a server + client component split: server component validates token and if authenticated redeems the invite; if NOT authenticated renders `InviteAcceptClient` which provides magic link (OTP) and Google OAuth sign-in on the same page. The invite token is preserved through auth via `emailRedirectTo` (magic link) or `redirectTo` (OAuth) both pointing to `/auth/callback?next=/invite/accept?token=X`. After auth, callback redirects to invite accept, server component redeems.
- Invite accept page MUST set `selected_facility_id` cookie after inserting facilityUser — without it, layout falls back to `?? 'admin'` and the invited stylist sees the admin view. IMPORTANT: cookies cannot be set in Server Components (Next.js 16). The invite accept page.tsx validates the token and auth, then `redirect('/api/invite/redeem?token=X')` to a Route Handler which does the DB work + sets the cookie. Route Handlers CAN set cookies. CRITICAL: middleware.ts must allow `/api/invite/redeem` through for users without a facilityUser (line 83 bypass) — without it, the redirect from page.tsx loops infinitely because middleware blocks the API route and sends the user back to /invite/accept.
- Invite accept auto-link: use `ilike(stylists.name, userFullName)` to match stylist by name — role === 'stylist' redirects to /my-account?welcome=1, others to /dashboard.
- Invite resend: POST /api/invites/[id]/resend — re-sends the invite email for unused, non-expired invites. Uses the same `buildInviteEmailHtml()` template exported from `/api/invites/route.ts`. Settings UI shows "Resend" button on pending non-expired invites.
- Remove Access: DELETE /api/facility/users/[userId] — admin-only. Guards: cannot remove yourself, cannot remove last admin. Runs a single db.transaction() that: (1) deletes the facilityUsers row, (2) clears `profiles.stylist_id` for the removed user (frees the stylist record so it can be re-linked if the user is re-invited), (3) marks any pending invites for that user's email at this facility as `used=true`. After the transaction, calls `supabase.auth.admin.signOut(userId, 'global')` via service-role admin client to invalidate sessions. Settings Team tab has two-step inline confirm (click "Remove" → "Remove? Yes No", mouse leave cancels). Local `localUsers` state removes the row optimistically; shows emerald toast "Access removed" for 3s.
- Invite revocation (DELETE /api/invites/[id]) cleans up: (1) pending access_requests for the same email+facility, (2) `profiles.stylist_id` if the revoked email has a stylist linked at this facility — checked by finding the profile by email and verifying the stylist belongs to this facility.
- Invite deduplication (POST /api/invites): before inserting, checks for existing invite at (email, facilityId). If `used=false` exists → refreshes token + expiry, resends email, returns `{ data, refreshed: true }`. If `used=true` exists → returns 409 "This person already has access to this facility". New insert only if no prior invite. UI shows "Invite refreshed and resent" when `refreshed=true`, "Invite sent!" for new. The `(email, facility_id)` pair does NOT have a unique DB constraint — dedup is enforced in the route handler.
- Settings Team tab shows linked stylist name (↔ StylistName) next to the role badge when `profiles.stylist_id` is set. `stylistName` is resolved in page.tsx via a batch `inArray` query against the stylists table and passed via the `ConnectedUser` interface.
- Settings Team tab shows status badges per user: "Active" (green, last sign-in < 90 days), "Invited" (amber, never signed in), "Inactive" (gray, > 90 days). `lastSignIn` fetched from auth.users via admin client in page.tsx.
- Settings Invites tab: expired invites show a "Re-invite" button that pre-fills the invite form email+role for one-click re-send.
- Middleware public/bypass routes: `/login`, `/auth`, `/unauthorized`, `/invite/accept`, `/portal/*`, `/api/portal/*`, `/invoice/*` are fully public. Additionally, `/onboarding`, `/invite/*`, and `/api/invite/redeem` are allowed through for authenticated users who have no facilityUser row — this is critical for the invite accept flow.
- Data cleanup: use `scripts/cleanup-*.mjs` pattern (delete after running) — `sql.unsafe()` for IN queries with UUID arrays via the postgres library.
- Super admin invite flow: POST /api/invites requires `facilityId` in body when caller is super admin (bypasses getUserFacility). GET /api/facilities returns all active facilities for super admin. Settings invites tab shows a facility dropdown when isSuperAdmin (detected via NEXT_PUBLIC_SUPER_ADMIN_EMAIL vs currentUserEmail prop).
- NavigationProgress: client component in src/components/ui/navigation-progress.tsx — uses usePathname to show 2px teal bar on route change. Imported in (protected)/layout.tsx.
- Log page stylist sections are collapsible — collapsed state keyed by stylistId in useState<Record<string, boolean>>({}).
- Role permission matrix:
  - **master_admin** (super admin): all facilities, all operations, /super-admin page, can assign facility on invite/access-request
  - **admin**: full access to own facility — calendar, log, residents, stylists, services, reports, settings, invites
  - **stylist**: calendar (/dashboard), daily log (/log), my account (/my-account) only. Mobile shows today-list filtered to own bookings. Log filters to own section. Can edit price/notes on own bookings only. Cannot access /residents, /stylists, /services, /reports, /settings.
  - **viewer**: read-only, no edits. Can see residents but not stylists/services/reports/settings.
- Stylist dashboard filtering: page.tsx looks up `profiles.stylistId`, passes `profileStylistId` to DashboardClient. Mobile today-list filters via `todayBookings.filter(b => b.stylistId === profileStylistId)`.
- Log inline price/notes editing: log-client.tsx has `editingBookingId` state. Edit button (pencil icon) on non-cancelled, non-finalized rows. Edit mode shows $ price input + notes textarea. Saves via PUT /api/bookings/[id] with `{ priceCents, notes }`. Stylist role: only sees edit button on own bookings (gated by `stylistFilter`). Admin: edit button on all bookings.
- PUT /api/bookings/[id] accepts `priceCents` directly in updateSchema. Direct priceCents override takes precedence over service-change priceCents. Stylist ownership guard: stylists can only edit their own bookings (checked via profiles.stylistId match).
- Mobile nav prefetch: `<Link prefetch={true}>` on all nav links in mobile-nav.tsx and sidebar.tsx. mobile-nav.tsx also has `pendingHref` state — set on click for immediate tab highlight, cleared on pathname change.
- loading.tsx files exist for /dashboard, /log, /residents — use existing skeleton components from src/components/ui/skeleton.tsx.
- **OCR loading screen**: `ocr-import-modal.tsx` replaces upload step content with an animated overlay when `scanning === true`. Pattern: `{step === 'upload' && (scanning ? (() => { ... })() : (<div ...>normal upload</div>))}`. Tip rotation: `useEffect` keyed on `scanning` runs a 3s `setInterval` — fade out (setTipVisible(false)) → 400ms later increment tipIndex + fade in. Progress bar: `scanProgress.match(/Scanning batch (\d+) of (\d+)/)` → `(X/Y)*100` capped at 90, default 5.
- **Full-page async overlay with two-phase progress bar** (`import-client.tsx` PDF parse): `parsing: boolean` + `progress: number` state. On PDF drop: `setParsing(true)`, `setProgress(0)`, then `setTimeout(() => setProgress(70), 50)` — the 50ms delay forces a browser paint at 0% before the phase-1 animation starts (without it, React batches `setProgress(0)` + `setProgress(70)` in one frame and the animation never plays). On Gemini response: `setProgress(100)`, `await new Promise(r => setTimeout(r, 400))`, then continue. `setParsing(false)` in `finally` so errors also dismiss the overlay. Progress bar `transition`: `progress === 70 ? 'width 2s cubic-bezier(0.4, 0, 0.2, 1)' : 'width 0.4s ease-out'` — slow/eased for phase 1, quick snap for phase 2. Overlay is `fixed inset-0 bg-black/40 backdrop-blur-sm z-50` with a centered `bg-white rounded-2xl shadow-xl` panel (rose-50 icon, two-line text, `h-1.5` bar).
- OCR log import: POST /api/log/ocr accepts multipart/form-data `images[]` (plural, multiple files allowed) + optional `servicesJson` (JSON string of `{ name, priceCents }[]`). The client batches files into chunks of 3 and sends sequential POST requests — each batch appends `servicesJson` so Gemini knows the facility's service names and prices. Route calls `buildInstruction(knownServices)` which injects the service list + `ABBREVIATIONS` expansion table into the Gemini prompt; Gemini uses price as a strong signal to expand shorthand like "S/BDry" → "Shampoo, Blow Dry". `maxDuration = 120`; per-file `Promise.race` timeout = 90s. Calls Gemini REST API **directly via `fetch`** — do NOT use the `@google/generative-ai` SDK (hardcodes v1beta with old model names). Model: **`gemini-2.5-flash`** on **`v1beta`** endpoint. Gemini 2.5 Flash is the current stable production model. Gemini 2.0 Flash is deprecated and unavailable to new API users. All Gemini 1.5 models shut down March 2026. Never use 1.5 or 2.0 model names. Gemini REST API uses **camelCase** field names — `inlineData`, `mimeType` — NEVER snake_case; the API silently ignores unknown fields. `systemInstruction` is NOT supported as a top-level field — fold the system prompt into the text part instead. Supports JPEG, PNG, WEBP, GIF, and **PDF** natively. Returns `{ data: { sheets: [{ imageIndex, date, stylistName, entries }] } }`. Route exports `maxDuration = 120` and `dynamic = 'force-dynamic'`.
- OCR import route: POST /api/log/ocr/import — creates residents (with random portalToken), services (durationMinutes defaults to 30), and bookings (status: 'completed', spaced 30 min apart from 09:00 UTC per sheet, i-th entry = 09:00 + i*30 min) inside a single db.transaction(). Requires admin or master admin. Inserts directly via Drizzle — does NOT call /api/bookings (intentionally bypasses conflict detection for historical imports). **3-step resident/service resolution before inserting**: (1) use provided UUID if given, (2) check in-memory `residentMap`/`serviceMap` (Map<string,string>, key = name.toLowerCase().trim()) for same-import dedup, (3) fuzzy-match against all existing active DB records loaded once before the transaction (`existingServices`/`existingResidents`) using `fuzzyScore() >= 0.8` — reuse the match if found, only insert new if no match. Both maps are updated and DB arrays are appended on new inserts so subsequent entries in the same import also benefit. This prevents duplicate services/residents across imports.
- OcrImportModal: `src/app/(protected)/log/ocr-import-modal.tsx`. 3-step flow: (1) Upload: multi-file picker (JPEG/PNG/WEBP/PDF), thumbnails, "Take Photo" camera button on mobile (`md:hidden`, `<input capture="environment">`); (2) Review: tab per sheet, collapsible source image (`<details>/<summary>`), date/stylist selectors, per-entry resident (text input + datalist) + service **select dropdown** (existing services grouped by category, `__new__${name}` option at bottom when no match); price/notes editing; Unclear badge; all inputs `min-h-[44px]` for mobile tap targets; (3) Confirm: summary counts, import. Sheet-level stylist is required — sheets with no stylist are skipped. Per-sheet scan errors shown individually. `fuzzyMatches()` uses two-pass logic: (1) substring includes in either direction, (2) normalized word-set overlap ≥80%. `fuzzyBestMatch(items, name, minScore=0.7)` uses `fuzzyScore()` to return the single best match above the threshold — used in `buildSheetState` to pre-select `serviceId` (excludes addon-type services) and `additionalServiceIds`. Addon-type services (`pricingType === 'addon'`) are excluded from primary service matching to prevent "S/BDry" → "Add on Deep Conditioner" false matches.
- **OCR price-aware service matching**: `buildSheetState` uses two signals — name fuzzy match + exact price match (`s.priceCents === ocrPrice`). Decision logic in an IIFE: if a single non-addon service has the exact OCR price AND the name score is < 0.85, prefer the price match (stronger signal). If a name match is found, trust it (price mismatch is expected when add-ons are bundled in the sheet total). If no name match, fall back to unique price match. This runs ONLY for primary services (addon-type excluded); add-on matching still uses name-only fuzzy.
- **OCR price is the source of truth**: the price written on the handwritten sheet must be preserved exactly. `buildSheetState` sets `priceCents: entry.price != null ? Math.round(entry.price * 100) : null` from Gemini's extraction. Service select `onChange` handlers NEVER include `priceCents` — only `serviceId` and `serviceName` are updated on service change. The Gemini prompt explicitly instructs to extract the EXACT written dollar amount and never substitute a catalog price. Price field in review UI shows a "from sheet" label below the input.
- OCR service name normalization: do NOT rename service names server-side before matching — bad renaming (e.g. "w/" → "wash") creates mismatched names in the DB. Instead, use `WORD_EXPANSIONS` in `normalizeWords()` at comparison time only: `{ w: 'wash', c: 'cut', hl: 'highlight', clr: 'color' }`. `normalizeWords()` lowercases, strips punctuation, splits, filters, expands shorthands, and sorts — so "w/ Curl" and "Wash, Curl" normalize to the same word set. `fuzzyScore(a,b)` = intersection.length / Math.max(aw.length, bw.length); also returns 0.85 for substring containment, 1.0 for exact. `fuzzyBestMatch(items, name, minScore=0.7)` returns the highest-scoring item above threshold. Service review fields use `<select>` (not free-text) — pre-selected via `fuzzyBestMatch` in `buildSheetState`. `__new__${name}` option value triggers new service creation; changing to an existing service ID sets `serviceId` and `serviceName`.
- Flexible pricing: services have `pricing_type` text column (default `'fixed'`). Four types: `fixed` (single price), `addon` (base + optional surcharge via `addon_amount_cents`), `tiered` (quantity-based via `pricing_tiers` jsonb), `multi_option` (named options via `pricing_options` jsonb). Bookings store `selected_quantity`, `selected_option`, `addon_total_cents` alongside the final `price_cents`. All price resolution goes through `src/lib/pricing.ts` — `resolvePrice()`, `formatPricingLabel()`, `validatePricingInput()`.
- `pricing_type` on services is `text()` in Drizzle (not a Postgres enum) — the `PricingService` interface in `src/lib/pricing.ts` uses `string` (not `PricingType`) for the `pricingType` field to avoid TS assignment errors from Drizzle's `text()` inference. Always use `PricingService` when accepting Drizzle query results.
- `price_cents` on bookings is ALWAYS the final fully-resolved total including add-ons, tier calculations, or option prices — never store a partial price. The server resolves the price via `resolvePrice()` before insert. Duration is NEVER affected by pricing type, add-ons, quantity, or options — always use the primary `duration_minutes` from the service.
- PDF parser (`/api/services/parse-pdf`) returns pricing hints: `pricingType`, `addonAmountCents`, `pricingTiers`, `pricingOptions` alongside name/priceCents/durationMinutes/category/color. Detects "add $X to service amount" (addon), "X-Y $Z ea. N or more $W ea." (tiered), and "-or-" patterns (multi_option). Services with "ea." suffix are flagged as multi_option for admin review. All imported services default to `pricingType: 'fixed'` unless a pattern is detected.
- POA fields on residents: `poa_name`, `poa_email`, `poa_phone`, `poa_payment_method` nullable text columns. Update schema.ts + PUT /api/residents/[id] updateSchema + Resident type in src/types/index.ts. POA section in resident-detail-client.tsx (edit + display). POA badge on resident list cards.
- POA booking flow: `GET /api/portal/[token]` exposes `poaName`/`poaEmail`/`poaNotificationsEnabled` on resident object. `portal-client.tsx` accepts `poaName?`/`poaEmail?`/`poaNotificationsEnabled?` props; shows teal-50 banner "Booking on behalf of {name}" when poaName is set. `POST /api/residents/[id]/send-portal-link` sends portal invite email to `poaEmail` — requires `poaEmail` and `portalToken` on resident. "Send portal link" button in resident detail Portal Link section, only shown when `resident.poaEmail` is set.
- POA email opt-in: `poa_notifications_enabled boolean NOT NULL DEFAULT true` on residents. Staff toggle in resident detail edit mode (checkbox "Send booking confirmations to POA") + "Confirmations on/off" badge in display mode. `PATCH /api/portal/[token]/notifications` — public route, updates the flag by token. Portal shows "Notification Preferences" section at bottom when `poaEmail` is set (fire-and-forget toggle). Both POA email gates (`POST /api/bookings` + `POST /api/portal/[token]/book`) check `poaNotificationsEnabled !== false` before sending.
- **Portal service picker** (`portal-client.tsx`): Card-based service selection — tapping a card selects it (burgundy border + check) without advancing to the next step. Grouped by `category` via `groupByCategory()` inline helper. Color swatch dot from `service.color`. **Auto-collapse**: `pickerOpen: Record<number, boolean>` state per slot. `setServiceAt()` auto-collapses to compact rose-50 summary row (color dot + name + price + "Change" link) after 150ms when only 1 service row exists; multiple rows stay open. Reset `pickerOpen` to `{}` on `startBooking()`. **Section order after first selection**: (1) selected service summary row(s), (2) tiered stepper / multi-option picker, (3) `+ Add another service` dashed button (`min-h-[56px]`, capped at `Math.min(4, nonAddonServices.length)`), (4) add-ons checklist, (5) live price breakdown, (6) Continue button. Continue button disabled if empty slot or multi-option not yet selected. All price computation via `resolvePrice()` from `@/lib/pricing` — never inline math.
- **Portal book route** (`/api/portal/[token]/book`): Zod schema accepts `serviceId?` OR `serviceIds[]` + `addonServiceIds[]` + `selectedQuantity?` + `selectedOption?`. Resolves price via `resolvePrice()`. Fetches addon services + additional primary services with `inArray`. Inserts booking with `serviceIds`, `priceCents` (total), `durationMinutes` (total), `selectedQuantity`, `selectedOption`, `addonServiceIds`, `addonTotalCents`. POA email uses resolved `totalPriceCents`.
- `buildBookingConfirmationEmailHtml()` in `src/lib/email.ts` — teal-header HTML email with Resident/Service/Stylist/Date/Time/Price table + "View in Portal" button. `bookedBy: 'staff' | 'portal'` controls the footer note. Fires from both `POST /api/bookings` (staff) and `POST /api/portal/[token]/book` (portal) when `poaEmail` + `portalToken` are set. Always fire-and-forget `.catch(console.error)`.
- Bookkeeper CSV export: `GET /api/export/bookkeeper?month=YYYY-MM` — admin-only, completed bookings only, facility timezone for date formatting. Columns: Date, Resident Name, Room #, Service, Stylist, Duration (min), Price, Payment Status, Payment Method (`resident.poaPaymentMethod`), Notes. "Bookkeeper CSV" `<a>` link in Reports invoice tab action bar alongside Generate Invoice + Mark All Paid.

### Compliance Documents (Phase 7)
- **Storage uploads go through `/api/compliance/upload` only** — multipart POST, server validates 10MB/PDF|JPEG|PNG, uploads to private `compliance-docs` bucket via service-role client. Service-role key NEVER touches the browser.
- **`compliance_documents.file_url` stores the storage PATH** (`{facilityId}/{stylistId}/{documentType}-{timestamp}.{ext}`), not a URL. `GET /api/compliance?stylistId=` regenerates a signed URL (1-hour TTL) per row on every read — signed URLs are never persisted.
- **Verify = admin only** (`PUT /api/compliance/[id]/verify`). Runs inside `db.transaction()` when mirroring to stylist columns: `license` → `licenseExpiresAt`; `insurance` → `insuranceVerified=true` + `insuranceExpiresAt`; `background_check` → `backgroundCheckVerified=true`. Unverify (`/unverify`) clears `verified`/`verifiedBy`/`verifiedAt` but does NOT roll back the stylist mirror columns — admin edits explicitly if needed.
- **Delete permission**: admin any; stylist role only on their own docs AND only while `verified = false`.
- **Status helper** `computeComplianceStatus(stylist, docs)` in `src/lib/compliance.ts` — required types are license + insurance. red = expired or required-type missing/unverified; amber = ≤60d OR any unverified doc; green = all good + >30d; none = no signals. Dot colors: green `bg-emerald-500`, amber `bg-amber-400`, red `bg-red-500`, none = no dot.
- **Doc type palette** (badges in My Account + Stylist Detail): license `bg-blue-50 text-blue-700`, insurance `bg-purple-50 text-purple-700`, w9 `bg-stone-100 text-stone-700`, contractor_agreement `bg-stone-100 text-stone-700`, background_check `bg-emerald-50 text-emerald-700`.

### Availability & Coverage (Phase 8)
- **Availability PUT is a full-week atomic replace** inside `db.transaction()` — delete all rows for the stylist, then insert the submitted 7 rows (active + inactive, so GETs always return a stable 7-day array). Never a partial upsert. Active rows require `startTime < endTime`; Zod `.max(7)` on the array.
- **Coverage POST derives `stylistId` server-side** from `profiles.stylistId` — NEVER trust `stylistId` from the request body. Past dates rejected. 409 on duplicate open request for the same `(stylistId, requestedDate)`.
- **Coverage status transitions**:
  - Admin: open → filled (requires `substituteStylistId`; verifies substitute is in facility + active + ≠ requester; sets `assignedBy`/`assignedAt`; fires `buildCoverageFilledEmailHtml`); open → cancelled; filled → cancelled (clears substitute fields); anything → open.
  - Stylist: may only submit `{ status: 'cancelled' }` on their own `open` request; any other body or status returns 403.
- **Coverage GET scoping**: stylist-role callers are ALWAYS forced to `stylistId = profile.stylistId` regardless of a `?stylistId=` query param. Viewer-role 403.
- **Coverage DELETE**: admin any; stylist-owner only while `status='open'`. Hard delete (not `active=false`) — coverage requests are transient.
- **Drizzle named relations**: `coverageRequestsRelations` joins to `stylists` twice via `relationName: 'coverage_stylist'` + `'coverage_substitute'`; `stylistsRelations` mirrors both sides. Two relations on the same target table REQUIRE named `relationName` on both sides or Drizzle throws at query time.
- **Emails are fire-and-forget**: `buildCoverageRequestEmailHtml` + `buildCoverageFilledEmailHtml` both called via `sendEmail(...).catch(console.error)` — never `await`.
- **Coverage banner + queue UI**: the amber banner below the pending-requests banner reuses its exact class signature (`shrink-0 px-4 py-2.5 rounded-2xl bg-amber-50 border border-amber-200 flex items-center justify-between`). Coverage Queue card lives above the tabs in the dashboard right rail with `id="coverage-queue"`; the banner's "View" button scrolls there via `scrollIntoView({ behavior: 'smooth' })`. Queue rows are hidden when empty to keep the rail tight; assignment optimistically removes the row from local state without `router.refresh()`.

### Phase 8.5 — Franchise Directory & Portal Auto-Assignment
- **`stylists.facility_id` is NULLABLE** — `NULL` means "franchise-pool" (unassigned, available as a substitute across the franchise). Any new query that lists stylists MUST explicitly choose a scope: `facility` (`eq(facilityId, X)` — silently excludes pool rows, this is the default and usually correct), `franchise` (`franchiseId = F AND facilityId IS NULL`), or `all` (OR of both). The `?scope=` param on `GET /api/stylists` makes this choice explicit at every call site.
- **`stylists.stylist_code` is NOT NULL + UNIQUE**, format `^ST\d{3,}$`. Generated via `src/lib/stylist-code.ts` → `generateStylistCode(tx)` which uses `pg_advisory_xact_lock(9191)` INSIDE a `db.transaction()`. The helper NEVER opens its own connection — all callers must pass the surrounding `tx`. Do NOT replace with a Postgres sequence (spec wants max+1 semantics so gaps don't grow, and custom codes like `ST500` don't break a sequence's monotonic contract).
- **Master admin only for `stylistCode` edits on existing rows.** Non-master admins can only set `stylistCode` at creation (honored by POST if passed; generated otherwise). PUT silently drops `stylistCode` from non-master requests.
- **Portal `/book` NEVER accepts a client-supplied `stylistId`.** The field is ignored if sent. `resolveAvailableStylists()` (`src/lib/portal-assignment.ts`) runs server-side: active + facility + availability for DoW + not on coverage + no overlapping booking. Picker chooses the candidate with the fewest non-cancelled bookings on the target date. 409 when zero candidates.
- **Portal `/available-times` returns only `availableSlots`** (slots with ≥1 candidate). The new `/available-days?month=` endpoint drives the date picker's greyed-out days. Both are public (token auth), rate-limited under the existing `portalBook` bucket.
- **Coverage is a DATE RANGE**: `start_date` + `end_date` (CHECK `end_date >= start_date`) replaced `requested_date`. Overlap-based duplicate-open rejects use `new.start ≤ existing.end AND new.end ≥ existing.start`. UI formats as `Jun 3 – Jun 7` when dates differ, single date when equal.
- **Coverage substitute picker has two groups**: facility pool (facility stylists with DoW availability, not themselves on coverage that date) + franchise pool (`facilityId IS NULL AND franchiseId = caller's franchise`, active). `GET /api/coverage/substitutes?date=YYYY-MM-DD` (admin-only) returns both. PUT `/api/coverage/[id]` accepts substitutes from either group.
- **Stylist import cap is 200 rows** (smaller than residents/services at 500 because per-row validation is heavier — facility resolution, optional license data). Returns `{ data: { imported, updated, availabilityCreated, scheduleNotes, errors: [{ row, message }] } }`.
- **Import facility default is franchise pool** (`resolvedFacilityId = null`). Stylists imported from a CSV with no `facilityName` column land in the franchise pool (`facilityId = null`), NOT pinned to the admin's current facility. Only when a `facilityName` column is present AND fuzzy-matches a franchise facility does a specific `facilityId` get set.
- **`POST /api/stylists/bulk-delete`** — admin-only, body `{ ids: string[] }` (max 200 UUIDs). Verifies each stylist is in the caller's franchise scope before deactivating. Uses soft-delete (`active: false`) per CLAUDE.md "NEVER hard delete" rule. Returns `{ data: { deleted: number } }`.
- **Applicant ZIP radius search**: `src/lib/zip-coords.ts` — static `ZIP_COORDS: Record<string, {lat, lng}>` table (~1200 entries covering DC/MD 20xxx-21xxx, VA 22xxx-23xxx, MN Twin Cities 55xxx). Helpers: `getZipsWithinMiles(zip, miles): string[]` (Haversine O(n) scan), `extractZip(location): string | null` (regex `\b\d{5}\b`). In `directory-client.tsx`: `appRadiusMiles` state (default 15); `filteredApplicants` useMemo computes `nearbyZips = getZipsWithinMiles(q, appRadiusMiles)` once when `q` is 5 digits, passes it to `appMatchesSearch(a, q, nearbyZips)`; toolbar shows radius `<select>` (5/10/15/25/50 miles) when search is exactly 5 digits; dep array includes `appRadiusMiles`.
- **`POST /api/stylists/bulk-update`** — admin-only, body `{ ids: string[], status?: StylistStatus, facilityId?: string, commissionPercent?: number }`. Same franchise-scope verification as bulk-delete. Exactly one of status/facilityId/commissionPercent must be provided (enforced by Zod `.refine`). `facilityId` path: verifies target facility is in franchise, then runs a `db.transaction()` that upserts `stylistFacilityAssignments` (UNIQUE on stylistId+facilityId) + updates `stylists.facilityId`. Returns `{ data: { updated: number } }`.
- **Directory multi-select**: checkboxes on each row + select-all in header; selected rows highlight `bg-rose-50`; floating action bar (`fixed, z-40, bottom: calc(env(safe-area-inset-bottom) + 80px), flex-wrap`) appears when `selected.size > 0`. Bar contains: Set Status dropdown, Set Facility dropdown, Set Commission % input, Apply button (burgundy), Delete button (red), Clear button. Changing any one field auto-clears the other two. Apply disabled when all three are empty. Optimistic local state updates — no `router.refresh()`. "Last Name" sort header (client-side `.pop()` on name — no server `orderBy`).
- **Directory status badge**: `STATUS_BADGE` constant defines label + className for `on_leave` (amber-50/amber-700/amber-200 border), `inactive` (stone-100/stone-500/stone-200 border), `terminated` (red-50/red-600/red-200 border). Badge uses `rounded-full text-[10px] font-semibold px-1.5 py-0.5`. Wrapped in `<span className="w-20 shrink-0 flex items-center">` for column alignment. Only shown when `status !== 'active'`. **Status** is a sortable column in the sort header (`w-20 shrink-0`); `sortKey` type includes `'status'`; sort useMemo uses `localeCompare` on `stylist.status ?? 'active'`.
- **Bookkeeping CSV column mapping** (import route): FNAME/LNAME → `name` (joined), `%PD`/`%paid` → `commissionPercent` (strip %, float parse, round, clamp 0–100), `How PD`/`howpaid` → `paymentMethod`, `License ST`/`licensestate` → `licenseState` (separate from `licenseType`), `SCHEDULE` → Gemini parse + availability insert, `ZIP`/`zipcode` appended to `address` if not already present. Bank/SSN fields (`bankact`, `bankrout`, `bankname`, `ssid`, `ssn`, `socialsecuritynumber`) silently skipped — never stored.
- **Bookkeeping CSV header detection**: PapaParse is run with `header: false` first (raw array of arrays). `detectHeaderRow()` scans the first 5 rows for signal columns (`lname`, `fname`, `id`, `name`, `stcode`, `stylistcode`, `%pd`, `schedule`) to skip leading garbage rows. Falls back to PapaParse `header: true` if no signal row found within the first 5.
- **Lenient import validation**: bad email format → drop silently (set to undefined before Zod); wrong `stylistCode` format → drop silently (auto-generate instead); phone → parsed to `phonesRaw` via `parsePhones()`, not part of Zod schema. Only missing name is a hard fail. This prevents one bad field from blocking an entire CSV row.
- **`stylists.phones` is jsonb `Array<{label: string, number: string}>`** — NOT NULL, default `[]`. Replaces the old `phone text` column. `parsePhones()` splits raw phone strings on ` or alternate `, ` or `, `/`, `&` (case-insensitive). Each part → `{ label: 'mobile', number: part.slice(0, 50) }`. In Stylist Detail: fully editable section with label dropdown (mobile/office/home/work/fax/custom) + custom label text input + number input + ✕ remove button + `+ Add` button. Saved via `phones` in `PUT /api/stylists/[id]` Zod schema: `.array(z.object({label: z.string().max(50), number: z.string().max(50)})).max(10)`.
- **`license_state` vs `license_type`**: `licenseState` = which state(s) the stylist holds a license in (e.g. `"MD, VA"`). `licenseType` = the type of license (e.g. `"Cosmetology"`). They are separate columns and separate UI fields. In Stylist Detail: `licenseState` is labeled "Licensed In". On the Stylists list: badge renders `licenseState.split(',').map(s=>s.trim()).join(' • ')` (e.g. `"MD • VA"`).
- **Gemini SCHEDULE parsing in import**: fires ONE batched text request with all schedule texts indexed (`0: text, 1: text, ...`) BEFORE entering the `db.transaction()`. Returns `{"0": [{facility, days[]}], "1": [], ...}`. On any parse failure, raw schedule text stored in `scheduleNotes`. Availability rows inserted via `onConflictDoNothing()` targeting `UNIQUE(stylistId, dayOfWeek)`. Day-level conflicts (two facilities claim the same day) → first match wins, second stored in `scheduleNotes`. `maxDuration = 300` on import route (single batched Gemini call can be large).
- **Circular type inference on self/cross-referencing tables**: `stylists.franchiseId` references `franchises`, but `franchises` also declares `stylists: many(...)` — this creates a circular type dependency that makes Drizzle infer every column as `any`. Fix by annotating: `franchiseId: uuid('franchise_id').references((): AnyPgColumn => franchises.id)`. Same pattern as the self-referential `bookings.recurringParentId`.

### Phase 9 — Stylist Assignments, Notes, Status, Specialties (schema foundation)
- **`stylists.status` vs `stylists.active`**: `active` is the soft-delete flag (never hard delete); `status text NOT NULL default 'active'` with CHECK `status IN ('active','inactive','on_leave','terminated')` is the lifecycle status. Treat them independently — deactivating (soft-delete) flips `active`; "on leave" flips `status`. UI/API come in Prompt 2+.
- **`stylists.specialties`** is `jsonb NOT NULL default '[]'` typed as `string[]` via Drizzle's `.$type<string[]>()`. Edits (add/remove chips) land in Prompt 2+.
- **`stylist_facility_assignments`** holds per-facility commission overrides. `commission_percent` is **nullable**: `NULL` means "use `stylists.commission_percent` default". Use `resolveCommission(stylistDefault, assignment)` from `src/lib/stylist-commission.ts` — it uses `!= null` (not truthy) so `0` is a valid explicit override. `UNIQUE(stylist_id, facility_id)`. `ON DELETE CASCADE` on both FKs. Backfill from existing `stylists.facility_id` + `stylists.commission_percent` into assignment rows is an **explicit separate step** — not done in Prompt 1.
- **`stylist_notes`** is admin-only internal notes (`body text NOT NULL`, `author_user_id → profiles.id`). Never expose via portal or stylist-role routes. Hard delete is fine — no `active` column.
- **`stylist_availability` UNIQUE is now `(stylist_id, facility_id, day_of_week)`** (was `(stylist_id, day_of_week)`). A stylist may declare different hours on the same day at different facilities. Full-week atomic replace inside `db.transaction()` still applies.
- **Assignments + Notes API** (Phase 9 Prompt 2): all four routes are admin-only. Assignment POST uses `.onConflictDoUpdate({ target: [stylistId, facilityId], set: {...} })` so it is a true upsert. Commission `null` = "use stylist default" — always display via `resolveCommission(stylist.commissionPercent, row)`. Notes `authorUserId` is ALWAYS derived server-side from `user.id`, never from the request body. Notes GET/POST join `profiles.email` so the UI shows `authorEmail` without a second fetch.
- **Stylist Detail UI** — status dropdown and specialties chips are part of `handleSave` (same PUT /api/stylists/[id] call as name/color/commission). Assignments and notes are saved independently via their own API calls with optimistic local state updates — never `router.refresh()`.
- **`stylists.facility_id` is DEPRECATED (Phase 9 Prompt 3).** Every query that scopes stylists to a facility MUST innerJoin `stylist_facility_assignments` (`stylistId = stylists.id AND facilityId = X AND active = true`) AND filter `stylists.status = 'active'` on booking surfaces. `stylists.active = true` is still required everywhere (soft-delete). The column still exists as the franchise-pool marker (`facility_id IS NULL AND franchise_id = F`) and for legacy-data fallback in the compliance cron. Migrated surfaces: `portal-assignment.ts`, `GET /api/stylists`, all three `/api/portal/[token]/*` stylist-listing routes, `GET /api/coverage/substitutes` (facility pool only), `/api/cron/compliance-alerts` (fans out to admins of every assigned facility), `POST /api/bookings` stylist-in-facility guard, `/stylists` list page, and `/stylists/directory`.
- **`/api/portal/[token]/available-times` + `available-days`**: availability filter now also requires `eq(stylistAvailability.facilityId, resident.facilityId)` — the UNIQUE constraint is 3-column (`stylist_id, facility_id, day_of_week`) so per-facility hours are authoritative. Same in `resolveAvailableStylists()`.
- **`GET /api/stylists?scope=all`** (Directory surface): returns the UNION of (A) every stylist with an active assignment at any facility in the caller's franchise + (B) franchise stylists with NO active assignment anywhere (unassigned pool). Does NOT filter `stylists.status` — Directory must show every lifecycle state so admins can see on-leave/inactive. `scope=facility` DOES filter `status='active'` (booking surface).
- **Compliance cron** (`/api/cron/compliance-alerts`) fans out one alert per assigned facility per stylist. Legacy `stylists.facility_id` is only consulted as a fallback when a stylist has zero active assignments (so existing data without assignment rows still alerts).
- **Directory `/stylists/directory` status UI**: second pill row with "All / Active / On Leave / Inactive" (parallel to the existing assigned/unassigned filter). Each stylist row shows a pill next to the name when `status !== 'active'` — `on_leave` amber, `inactive` stone-100, `terminated` stone-200. `/stylists` (facility booking surface) never shows non-active stylists, so no badge there.
- **Directory "Add Stylist" inline form** is wrapped in `<form onSubmit>` with the submit button typed `submit` and the Cancel button typed `button`. Enter-key submission on any input works this way. Do NOT use `pattern="..."` on the ST code input — HTML-native pattern validation silently blocks submission with no user feedback; rely on Zod server-side. `autoFocus` on the name input.
- **`stylist_facility_assignments` backfill** (Phase 9 Prompt 4): run `scripts/backfill-stylist-assignments.mjs` once to seed from existing `stylists.facility_id` rows — inserts with `ON CONFLICT DO NOTHING`. Delete the script after running. 2 rows inserted in initial run.
- **`stylists.last_invite_sent_at` timestamptz** (Phase 9 Prompt 4): added via one-off migration script. Used by `POST /api/stylists/[id]/invite` to rate-limit invites to one per 24h. Updated server-side after a successful Supabase Admin invite call. Never trusted from client.
- **Stylist invite flow** (`POST /api/stylists/[id]/invite`): admin-only. Guards: email required (400), no linked profile (409 if exists), franchise scope (403 if outside), 24h rate limit (429). Uses `supabaseAdmin.auth.admin.inviteUserByEmail(email, { data: { stylist_id }, redirectTo: APP_URL/invite/accept })`. Updates `lastInviteSentAt` after success. UI shows "Account linked ✓" (when `hasLinkedAccount`), "Invite sent Nh ago" (within 24h), or "Send account invite →" button (otherwise). `inviteStatus: 'idle'|'sending'|'sent'|'error'` state in StylistDetailClient.
- **`formatHHMM(t: string): string`** — file-local helper in `dashboard-client.tsx` and `my-account-client.tsx` for formatting `"09:00"` → `"9:00am"`. Do NOT use `formatTime()` from utils.ts for HH:MM strings — it expects a full Date/datetime string and produces wrong output for bare time strings.
- **"Who's Working Today" dashboard card** (Phase 9 Prompt 4): admin-only right-rail card above Coverage Queue. Fetched in `dashboard/page.tsx` via double innerJoin (`stylistFacilityAssignments` + `stylistAvailability` for today's dayOfWeek). Shows color dot + name + formatted hours per stylist. "Tomorrow:" line shows names for next day's availability. Card hidden when both `workingToday` and `workingTomorrow` are empty.
- **My Account "Your Schedule"** (Phase 9 Prompt 4): replaces the flat 7-day checkbox grid. Display order Mon–Sun (dayOfWeek `[1,2,3,4,5,6,0]`). For each day: if `days` state has an `active` entry → show facilityName (looked up from `stylistAssignments` via `facilityId`) + formatted hours + [Edit hours] inline button. No active entry → "— not scheduled" (read-only). Edit opens two `<input type="time">` per day; save reconstructs full 7-day array and calls existing `PUT /api/availability` (full-week atomic replace — no API change needed). `DayRow` type includes `facilityId?: string` so the facility name lookup works.

### Payroll (Phase 10A)
- **Three tables** — `pay_periods`, `stylist_pay_items`, `pay_deductions`. All have RLS enabled + `service_role_all` (USING true WITH CHECK true). NO `authenticated` policies — admin UI fetches via `/api/pay-periods/*` (service-role Drizzle). If browser-direct reads are ever needed, add a scoped policy later.
- **CHECK constraints live in the DB only**, validated server-side via Zod enums — mirrors the `stylists.status` precedent. Enums: `period_type ∈ {weekly,biweekly,monthly}`, `status ∈ {open,processing,paid}`, `pay_type ∈ {commission,hourly,flat}`, `deduction_type ∈ {cash_kept,supplies,advance,other}`.
- **`stylist_pay_items.hours_worked`** is `numeric(6,2)` — Drizzle returns numeric columns as **strings** (Postgres preserves precision). Always `parseFloat(hoursWorked ?? '0')` on read; write via `String(value)`.
- **`stylist_pay_items` UNIQUE**: `(pay_period_id, stylist_id)` named `stylist_pay_items_period_stylist_unique`. CASCADE on `pay_period_id → pay_periods.id` and `pay_deductions.pay_item_id → stylist_pay_items.id`.
- **Net pay formula** = base − Σ deductions, `Math.max(0, …)`. Base: commission → `commissionAmountCents`; hourly → `Math.round(hoursWorked × hourlyRateCents)`; flat → `flatAmountCents`. Helper: `computeNetPay(item, deductions)` in `src/lib/payroll.ts` — NetPayInputs interface uses `payType: string` (not the union) because Drizzle `text()` returns `string`.
- **Recompute on every mutation**: item PUT, deduction POST, deduction DELETE all re-fetch current deductions and persist `netPayCents` inside a single `db.transaction()`. Never store a stale net pay.
- **Period creation** (`POST /api/pay-periods`): admin-only, rate-limited `payPeriodCreate` (10/hr). Zod: enum validation + `endDate >= startDate` + notes max 2000. In `db.transaction()`: (1) insert period, (2) fetch active `stylist_facility_assignments` INNER JOIN stylists WHERE `facilityId=X AND assignment.active=true AND stylist.active=true AND stylist.status='active'`, (3) scan completed bookings `WHERE facility_id=X AND stylist_id=S AND status='completed' AND start_time >= startDate AND start_time < (endDate + 1 day)` and sum `price_cents` (null = 0 per stats-route precedent), (4) compute rate via `resolveCommission(stylist.commissionPercent, { commissionPercent: assignment.commissionPercent })`, (5) batch-insert pay items with `netPayCents = commissionAmountCents` (no deductions yet). `maxDuration = 60`, `dynamic = 'force-dynamic'`.
- **`paid` locks the period.** Any item PUT / deduction POST / deduction DELETE returns 403 when the period is `paid`. Status transitions are forward-only: `open → processing → paid`. UI disables all inline edit controls when paid.
- **`revalidateTag('pay-periods', {})`** on every mutation (two-arg form per Next.js 16). Applies to: POST/PUT period, PUT item, POST/DELETE deduction.
- **Sanitize nested stylists**: detail route returns items with `stylist` joined — always `sanitizeStylist(row.stylist)` and wrap the full payload in `toClientJson()` before returning to the client (strips `googleRefreshToken` + any other sensitive keys).
- **Rate limit buckets**: `payrollExport` 20/hr/user, `payPeriodCreate` 10/hr/user.
- **CSV export** (`GET /api/pay-periods/[id]/export`): admin-only, rate-limited, `maxDuration = 60`. Columns: Stylist Name, ST Code, Facility, Pay Type, Gross Revenue, Commission Rate, Commission Amount, Hours Worked, Hourly Rate, Flat Amount, Deductions, Net Pay, Notes. Dollars to 2 decimals; rate as `55%`; total row appended. Filename `payroll-{start}-{end}.csv`. Reuse the `escapeCsv` pattern from the bookkeeper export.
- **Item PUT validation**: verify `item.payPeriodId === route [id]` (defense against ID shuffling); reject when period paid; Zod caps: `hoursWorked >= 0`, `hourlyRateCents 0..10_000_000`, `flatAmountCents 0..10_000_000`, notes max 2000.

### QuickBooks (Phase 10B)
- **`facility_code`** is the cross-system identifier (e.g. `F120`) matching QB customer codes and Google Drive folder names. Nullable text on `facilities` with a partial unique index. Set automatically on QB import (same value as `qbCustomerId`). Can be manually set for non-QB facilities. Shown as a small monospace badge (`bg-stone-100 text-stone-500 font-mono`) next to the facility name in sidebar, settings subheader, settings General tab (read-only field), and super-admin facility list. Sidebar uses `bg-white/10 text-white/50` for the dark background. Layout.tsx passes `facilityCode` to `<Sidebar>` alongside `facilityName`. Sidebar `FacilityOption` interface includes `facilityCode?: string | null`.
- **QB Customer Import**: `facilities.qbCustomerId` and `residents.qbCustomerId` are the deduplication keys for the QB customer import tool. Both are nullable `text` columns with partial unique indexes (`WHERE qb_customer_id IS NOT NULL`). Route: `POST /api/super-admin/import-quickbooks` (master admin only, `maxDuration=120`, parses XLS/XLSX with the `xlsx` library). Idempotent — safe to re-run; updates fields on existing records if QB customer ID matches. UI at `/super-admin/import-quickbooks` (3-state: upload → loading overlay → results summary with warnings collapsible). `residents` also has `poaAddress` and `poaCity` (nullable text). The `Street Address` QB column is parsed: `C/O Name` prefix → `poaName`; optional second line → `poaAddress`; non-C/O value → `poaAddress` directly. `City` column → `poaCity`. **Performance pattern**: the route uses 2 bulk SELECTs (one for facilities, one for residents) to build Maps, then separates into inserts vs updates — chunked batch inserts (50 per chunk for both), individual UPDATEs via `Promise.all` in chunks of 50. No outer `db.transaction()` — avoids the 120s timeout that row-by-row `findFirst` queries would cause. `inArray()` calls are always guarded with a `.length > 0` check (Drizzle throws on empty array). **Dedup pattern**: facilities are deduped by `qbId` (last occurrence wins) before any DB work; residents are deduped by `qbCustomerId` globally, then also by `name+facilityId` within each chunk (prevents "ON CONFLICT DO UPDATE command cannot affect row a second time" when two QB entries share a name at the same facility). **Resident conflict target is `qbCustomerId`** (partial unique index `WHERE qb_customer_id IS NOT NULL`). The `UNIQUE(name, facility_id)` constraint (`residents_name_facility_id_unique`) was **dropped** — two different residents at the same facility can legitimately share a name (e.g. two people named "Mitsue Gaylor" in different rooms). `qb_customer_id` partial unique index is now the sole dedup mechanism for imported residents. Manual (non-QB) residents have no uniqueness constraint beyond their primary key. **Row detection — three facility formats**: (A) `F123 - Name` (standard dash), (B) `F223 Sholom Home West` (space-only, no dash), (C) `F127` (standalone code, inserted with `name=''` so residents can reference it). **Two resident formats**: (A) `F123:Last, First - Room`, (B) `F174 - Facility Name:Last, First - Room` (QB sub-customer with full name before colon). For both resident formats, `facilityQbId` is extracted from `^(F\d+)` and the resident portion is everything after the last colon. Resident format B rows would previously be misclassified as facilities (they contain a colon, but the earlier colon-guard rejected them as unrecognized). Rows matching no format are skipped with a warning.
- **QB tokens are sensitive and encrypted at rest** — `qbAccessToken` and `qbRefreshToken` are stored as AES-256-GCM ciphertext in the DB (via `src/lib/token-crypto.ts` `encryptToken()`). Key: `QB_TOKEN_SECRET` env var (32-byte hex, 64 chars, stored in Vercel). `sanitizeFacility()` drops both, surfaces `hasQuickBooks: boolean`. Server never returns tokens to the client. **`encryptToken()` and `decryptToken()` are called ONLY inside `src/lib/quickbooks.ts`** — never in route handlers. Route handlers receive `encryptToken()`'d output from `exchangeQBCode()` callers and store it directly. Decryption happens in `doRefresh()` (before POST body) and `revokeQBToken()` (before Intuit revoke call).
- **Always call `refreshQBToken(facilityId)` before any QB fetch** — never read `facilities.qb_access_token` directly in a route handler. The helper refreshes 5min before expiry and dedupes concurrent refreshes per-facility via in-memory `Map<facilityId, Promise<string>>`. Use `qbGet(facilityId, path)` / `qbPost(facilityId, path, body)` which handle this automatically.
- **401 retry**: `qbFetch` clears the cached expiry and retries exactly once on 401, then throws. Never loop.
- **Sparse update pattern**: QB mutations that update an existing entity require `{Id, SyncToken, sparse: true}` — always GET the entity first to capture a fresh SyncToken, then POST the delta.
- **One Bill per stylist per pay period** — QB Bills have exactly one VendorRef. `stylist_pay_items.qb_bill_id` + `qb_bill_sync_token` track the per-item Bill; `pay_periods.qb_synced_at` is the aggregate last-push timestamp; `pay_periods.qb_sync_error` summarizes failures.
- **Expense account is mandatory** — `facilities.qb_expense_account_id` is required before Bill sync. UI blocks sync with 412 until admin picks one from `/api/quickbooks/accounts`.
- **Portal-level OAuth is per-facility** — `oauth_states.stylist_id` is now nullable; `facility_id` was added to the same table. Exactly one of (stylistId, facilityId) is populated per row. Google Calendar flow still populates stylistId; QB flow populates facilityId. The google-calendar callback now guards `if (!stateRow.stylistId) throw` since the column is nullable.
- **Rate limit**: `quickbooksSync` bucket 15/hr/user on all QB POST routes + `/api/quickbooks/accounts`. `maxDuration = 60` + `dynamic = 'force-dynamic'` on every QB route (vendor sync and bill sync can make 20+ API calls).
- **Revoke on disconnect** — POST `developer.api.intuit.com/v2/oauth2/tokens/revoke` fire-and-forget in addition to clearing DB columns. Helper `revokeQBToken()` in `src/lib/quickbooks.ts`.
- **`POST /api/quickbooks/sync-bill/[periodId]` auto-syncs vendors inline** for any stylist whose pay item has `netPayCents > 0` but no `qbVendorId` — reuses `syncVendorsForFacility(facilityId, filterStylistIds)` exported from the sync-vendors route. Period must be `processing` or `paid` (not `open`).
- **Payment status sync** (`POST /api/quickbooks/sync-status/[periodId]`) fetches all Bills for the period and, if every `Balance === 0` and status ≠ paid, flips the period to `paid` + `revalidateTag('pay-periods', {})`.
- **UI gate**: payroll QB panel is hidden when `facility.hasQuickBooks === false` OR `period.status === 'open'`. Settings integration card lives in the Integrations tab — `?qb=connected` / `?qb=error&reason=…` URL params trigger toasts once and clear themselves via `replaceState`.
- **CSP** `connect-src` includes `https://quickbooks.api.intuit.com https://oauth.platform.intuit.com https://appcenter.intuit.com https://developer.api.intuit.com` (server-side fetch isn't subject to browser CSP, but extend anyway for future client widgets).
- Env: `QUICKBOOKS_CLIENT_ID`, `QUICKBOOKS_CLIENT_SECRET` in Vercel (server-only, no `NEXT_PUBLIC_*`).

### Phase 11A — Billing AR Foundation
- **Three new tables** — all have RLS enabled + `service_role_all` policy:
  - `qb_invoices`: dedup unique index on `(invoice_num, facility_id)`. Status values: `open`, `partial`, `paid`, `credit`. Columns: facilityId, residentId (nullable), qbCustomerId, invoiceNum, invoiceDate, dueDate, amountCents, openBalanceCents, status, paymentType, qbInvoiceId, lastSentAt, sentVia, syncedAt.
  - `qb_payments`: natural key unique index `qb_payments_natural_key_idx ON (payment_date, facility_id, amount_cents, COALESCE(invoice_ref, ''))` — COALESCE prevents NULL bypass. `recorded_via` values: `manual`, `qb_import`. Columns: facilityId, residentId (nullable), checkNum, checkDate, paymentDate, amountCents, memo, invoiceRef, paymentType, recordedVia, checkImageUrl, qbPaymentId, resolvedAt, syncedAt.
  - `qb_unresolved_payments`: staging table for unmatched checks. Columns: facilityId, checkNum, checkDate, totalAmountCents, rawResidentName, rawAmountCents, rawServiceType, checkImageUrl, notes, resolvedToResidentId.
- **New columns**: `facilities.qb_outstanding_balance_cents` + `facilities.qb_rev_share_type` (default `'we_deduct'`). `residents.qb_outstanding_balance_cents` + `residents.resident_payment_type`.
- **Balance recompute pattern**: two `db.execute(sql\`UPDATE...\`)` correlated subqueries after any invoice import run — never store stale balance, always recompute from `SUM(open_balance_cents)`.
- **`import-billing-history` route** (`POST /api/super-admin/import-billing-history`): master admin only, `maxDuration=120`, rate-limited `billingImport` (5/hr). Accepts two optional CSV files — `invoices` and `transactions` (at least one required). Uses PapaParse. `parseCents(val)` strips commas, parseFloat, Math.round*100. `parseQBDate(val)` parses `MM/DD/YYYY` → `YYYY-MM-DD`.
  - **Invoice List** (`header: true`): skip non-Invoice rows, skip rows with empty Date. Derive facilityId from Name column — everything before `:` is the F-code. `onConflictDoUpdate` on `(invoice_num, facility_id)`. Status derived: `openBalance === 0 → 'paid'`, `< 0 → 'credit'`, `< amount → 'partial'`, else `'open'`. Dedup within batch by `invoiceNum+facilityId`.
  - **Transaction List** (`header: false`): find header row by scanning for columns containing "Date" and "Transaction type". Track `currentFacilityId` from col0 rows with F-code prefix. Only process `transactionType === 'Payment'` detail rows. `onConflictDoNothing()` without target — relies on natural key index. Extract checkNum from memo via `/(?:CK|CHK|CHECK)\s*#?\s*(\w+)/i`.
- **UI**: `/super-admin/import-billing-history` (3-state: upload → loading → results). Two separate `<input type="file" accept=".csv">` pickers (one for invoices, one for transactions). Import button disabled when both null. Results show 4 stats: invoices processed, payments processed, facilities with open balance, residents with open balance. "Import Billing History" link in super-admin header. Rate limit: `billingImport` 5/hr.
- **CSV metadata rows**: QB exports have 4 metadata rows before actual headers (title, company, date range, blank). Strip them by scanning for the real header line before passing to PapaParse. Invoice List: `lines.findIndex(l => l.trimStart().startsWith('Date,'))`. Transaction List: `lines.findIndex(l => l.includes(',Date,') && l.includes('Transaction type'))`.

### Phase 11B — AR Dashboard (`/billing`) [SHIPPED 2026-04-20]
- **Route**: `/billing` (under `(protected)` route group — NOT `(dashboard)`). Admin+ only; stylists/viewers redirect to `/dashboard`.
- **Role scope**:
  - `master_admin` (via `NEXT_PUBLIC_SUPER_ADMIN_EMAIL`) — facility `<select>` across all active facilities, switches data on change
  - `facility_admin` — locked to their own facility, no switcher rendered
  - stylist / viewer / other — redirect to `/dashboard`
- **API**: `GET /api/billing/summary/[facilityId]` — admin-guarded, enforces caller facility === requested facility for non-master, returns `{ data: { facility, residents, invoices, payments } }`. Every Drizzle query uses an explicit `columns:` whitelist — no tokens/secrets can leak (`qbAccessToken`, `qbRefreshToken`, `stripeSecretKey` are never selected).
- **View branching on `facilities.payment_type`** (in `billing-client.tsx`):
  - `'ip'` → `<IPView>` — per-resident 12-col grid: Resident / Room / Last Service / Billed / Paid / Outstanding / Last Sent
  - `'rfms'` or legacy `'facility'` → `<RFMSView>` — rev-share note card + Checks Received table (date / check# / amount / memo) + per-resident breakdown (resident/room/last service/billed/outstanding)
  - `'hybrid'` → `<HybridView>` — split panel. IP section: `residents.filter(r => r.residentPaymentType === 'ip')`. RFMS section: everything else (including `null`). Each section computes its own outstanding from filtered residents.
- **Shared helpers** (`src/app/(protected)/billing/views/billing-shared.tsx`): interfaces (`BillingFacility`, `BillingResident`, `BillingInvoice`, `BillingPayment`, `BillingSummary`, `ResidentTotals`), `formatDollars`, `formatInvoiceDate`, `formatShortDate`, `formatSentVia`, `revShareLabel`, `computeResidentTotals(resident, invoices)` (billed = SUM invoice.amountCents for this residentId; outstanding = resident.qbOutstandingBalanceCents; paid = billed − outstanding; lastServiceDate = MAX invoiceDate; lastSentAt/Via from the most recent invoice with `lastSentAt != null`), `DisabledActionButton`, `StatCard`.
- **Totals card**: Total Billed = SUM invoice.amountCents; Total Received = SUM payment.amountCents; Outstanding = `facility.qbOutstandingBalanceCents`. Outstanding > 0 uses `<StatCard highlight="amber">` (bg-amber-50 + text-amber-700); = 0 uses default stone palette.
- **Rev-share note**: only rendered when paymentType is `rfms`/`facility`/`hybrid`. Uses `revShareLabel(facility.qbRevShareType)`: `facility_deducts` → "Facility deducts revenue share before paying"; anything else → "Senior Stylist deducts revenue share".
- **Action buttons**: "Send Statement" is now a real active button wired in `billing-client.tsx` (Phase 11C). "Send via QB" remains `<DisabledActionButton>` ("Available after QB production approval"). Rendered above the main view.
- **Loading state**: two `animate-pulse bg-stone-100 rounded-2xl` skeletons (h-24 + h-64).
- **Empty state**: `summary.invoices.length + summary.payments.length === 0` → card with "No billing data yet. Import historical data from the Super Admin panel."
- **Sidebar nav**: Billing entry inserted **between Reports and Payroll** in `src/components/layout/sidebar.tsx` (admin-only). Inline SVG icon (document with dollar glyph, 16×16, strokeWidth 2). **Mobile-nav intentionally NOT updated** — the 5-icon bar is already full and `/payroll`, `/reports`, `/settings` are also absent from mobile.
- **Design tokens**: burgundy `#8B2E4A` primary (NOT `#0D7377` teal — CLAUDE.md rule), focus ring `ring-rose-100`, card shell `bg-white rounded-2xl border border-stone-100 shadow-sm`, page padding `p-4 md:p-8 max-w-6xl mx-auto`, heading `text-2xl md:text-3xl` inline `style={{ fontFamily: 'DM Serif Display, serif' }}`.
- **No mutations, no schema changes, no rate-limit changes** — read-only dashboard only.

### Phase 11C — Statement & Reminder Emails [SHIPPED 2026-04-20]
- **Three API routes** under `src/app/api/billing/send-statement/`:
  - `POST facility/[facilityId]` — facility-level statement email to `contactEmail`; requires admin or isMaster
  - `POST resident/[residentId]` — per-resident reminder to `poaEmail`; verifies `resident.facilityId` ownership
  - `POST facility/[facilityId]/all-residents` — batch sends to all residents with `qbOutstandingBalanceCents > 0` AND `poaEmail`; sequential (not parallel) to avoid Resend rate limits; `maxDuration=60`
- **Dedup pattern**: query `max(lastSentAt)` on `qbInvoices` for the scope; if within 7 days AND `!force` → return `{ warning: true, lastSentAt }` at HTTP 200; client re-POSTs with `{ force: true }` to bypass. `SendDedupModal` in `billing-shared.tsx` (stone overlay, Cancel + "Send Anyway").
- **Email templates** in `src/lib/email.ts`: `buildFacilityStatementHtml` (3-tile billed/received/outstanding + resident table + payment history + rev-share note + footer `pmt@seniorstylist.com · 443-450-3344`) and `buildResidentStatementHtml` (outstanding callout amber/green + invoice history). Both use inline styles only — no @react-email.
- **`await sendEmail()`** (NOT fire-and-forget) — `lastSentAt` + `sentVia = 'resend'` only updated after confirmed send.
- **`billingSend` rate-limit bucket**: 20/hr in `src/lib/rate-limit.ts`.
- **`BillingFacility`** extended: `contactEmail: string | null`, `address: string | null`. **`BillingResident`** extended: `poaEmail: string | null`. Both fields added to billing summary route column whitelists.
- **UI wiring**: billing-client.tsx global "Send Statement" button wired (shows inline email input when no `contactEmail`; dedup modal on warning; `refreshKey` increment forces re-fetch after send). IP view: "Send All (N)" header button (confirm before batch) + per-row Send/Resend buttons (hidden when no `poaEmail`). RFMS view: duplicate "Send Statement" removed (billing-client global covers it; "Send via QB" stays disabled).
- **QB path** ("Send via QB") stays disabled — pending 11F production approval.

### Phase 11C.5 — Billing Hub Redesign + Animation System [SHIPPED 2026-04-20]
- **`src/lib/animations.ts` is the single source of motion tokens.** Import constants (`btnBase`, `btnHubInteractive`, `cardHover`, `transitionBase`, `expandTransition`, `modalEnter`, `successFlash`, `shimmer`) — never re-declare raw transition classes elsewhere. `btnHubInteractive` adds `hover:scale-[1.02]` and is reserved for the billing hub (pill toggles, stat cards). Do NOT apply hover-scale site-wide.
- **Shared Button** (`src/components/ui/button.tsx`) uses `active:scale-[0.97] ease-out` — do NOT revert to `active:scale-95` (too harsh). Hover-scale is NOT applied to the shared button.
- **Skeleton loaders**: use `className="skeleton-shimmer rounded-2xl h-N"` (references `.skeleton-shimmer` in `globals.css` with its built-in sweep animation). Do NOT inline `animate-pulse bg-stone-100` — that skips the gradient shimmer.
- **Toast** (`src/components/ui/toast.tsx`) — EXISTING, reuse via `useToast()` hook with positional API `toast(message, type)` where `type ∈ 'success' | 'error' | 'info'`. Already wired via `<ToastProvider>` in `(protected)/layout.tsx`. Do NOT rewrite.
- **`useCountUp(target, duration=600)`** (`src/hooks/use-count-up.ts`) — animates to `target` via `requestAnimationFrame` + easeOutCubic. Seeds initial `value` to `target` (no hydration mismatch). Honors `prefers-reduced-motion: reduce` (jumps straight to target). Call at top level of component; for multiple animated values declare one hook per value.
- **`<ExpandableSection title meta defaultOpen children>`** (`src/app/(protected)/billing/views/expandable-section.tsx`) — shared accordion pattern. Max-height cap is 5000px (comfortably fits hundreds of rows). Chevron rotates via `transitionBase`. If a future surface needs > 5000px worth of rows the cap must be revisited; for now it's an acceptable trade-off vs JS-measured heights.
- **Billing hub card** (`billing-client.tsx`) — single card: facility name (serif) + `facilityCode` mono badge + payment-type pill (IP/RFMS/Hybrid), Send Statement button with inline spinner, 3 animated stat tiles (Billed/Received/Outstanding via `useCountUp`), rev-share pill toggle row (only when `paymentType ∈ {rfms, facility, hybrid}`). Save button appears only when `pendingRevShare !== null && pendingRevShare !== current`, wrapped in `successFlash`. Master admin also sees a 4-card cross-facility bar above the selector (Total Outstanding / Collected This Month / Invoiced This Month / Facilities Overdue). Rev-share toggle lives on the hub card — do NOT reintroduce the rose-50 notice in RFMSView.
- **Payment-type views expandable**: `IPView` wraps its residents block in one `<ExpandableSection>` (default open). `RFMSView` wraps Checks + Residents in two sections (both default closed). `HybridView` calls `IPView` (title="IP Residents", open) + `RFMSView` (residentsTitle="RFMS Residents", checksTitle="RFMS Checks received", both closed). All three accept optional `title` / `defaultOpen` overrides so hybrid can rename/recolor without duplicating internals.
- **`GET /api/billing/cross-facility-summary`** — master admin only (`NEXT_PUBLIC_SUPER_ADMIN_EMAIL`); 403 otherwise. Four `db.execute(sql\`...\`)` aggregates returning `{ totalOutstandingCents, collectedThisMonthCents, invoicedThisMonthCents, facilitiesOverdueCount }`. `maxDuration=30`, `dynamic='force-dynamic'`. **This project's postgres driver returns rows directly from `db.execute` (iterable / `result[0]`), NOT under `.rows` — always access `(result[0] as { col?: unknown })?.col`, never `result.rows[0]`.**
- **`PATCH /api/facilities/[facilityId]/rev-share`** — admin or master only; facility admins scoped to own facility. Zod body `{ revShareType: 'we_deduct' | 'facility_deducts' }`. No rate limit (low-frequency admin action). `dynamic='force-dynamic'`.

### Phase 11C.6 — Billing Polish [SHIPPED 2026-04-20]
- **`formatDollars` is an `Intl.NumberFormat` singleton** in `src/app/(protected)/billing/views/billing-shared.tsx`. NEVER re-implement with string concat — `Intl` handles thousands separators (`$316,796.94`) and negative signs (`-$1,234.56`) automatically. One module-level formatter instance feeds every billing surface.
- **Date range param convention**: `GET /api/billing/summary/[facilityId]?from=YYYY-MM-DD&to=YYYY-MM-DD`. Both params required when filtering (absence of EITHER = full history for back-compat). Zod regex `/^\d{4}-\d{2}-\d{2}$/` validates each; drizzle `and(eq(facilityId), gte(invoiceDate, from), lte(invoiceDate, to))`. Same filter applied to `qb_invoices.invoice_date` AND `qb_payments.payment_date`. Residents + facility queries are NEVER date-filtered.
- **Outstanding tile sourcing rule**: `totals.outstanding = facility.qbOutstandingBalanceCents` ONLY when `activePeriod === 'all'` (the authoritative 11C.5 value). For every other period (`month`/`year`/`custom`), derive from `sum(invoice.openBalanceCents)` over the filtered invoice array. Billed/Received ALWAYS derive from the filtered arrays — no conditional. Do NOT use `qbOutstandingBalanceCents` when a date range is active; it reflects full history and will disagree with the filtered invoices the user sees.
- **Date range pill toggle** on billing hub: 4 periods (`month` default, `year`, `custom`, `all`). Custom opens inline popover with From/To + Apply. `toISODate(d)` in `billing-client.tsx` uses LOCAL-timezone components (`getFullYear()`, `getMonth()+1`, `getDate()`) — NEVER `toISOString()` which shifts to UTC. Invalid range (`from > to`) returns early with a toast; no fetch fires.
- **`/billing?facility=ID` deep link** is supported. `billing-client.tsx` reads `useSearchParams()` inside the `useState` initial-value callback, ONLY overriding `initialFacilityId` when the param ID exists in `facilityOptions`. `page.tsx` MUST wrap `<BillingClient>` in `<Suspense fallback={null}>` — Next.js 16 requires a Suspense boundary around any client component that calls `useSearchParams()`.
- **Cross-facility drill-down pages are master-admin-only** (`/billing/{outstanding,collected,invoiced,overdue}`). Each page.tsx is a server component that redirects non-masters to `/billing` BEFORE try/catch. All four render the shared `CrossFacilityReportClient` with a different `type` prop. Row click → `router.push('/billing?facility=' + id)` — reuses the deep-link pattern. CSV export is client-side only (`Blob` + `URL.createObjectURL`); reflects the CURRENT client-side sort.
- **`GET /api/billing/cross-facility-detail?type=...`** — master-admin-only, Zod enum validation for `type`. Four `db.execute(sql\`...\`)` SQL variants. Rows accessed as ITERABLE (`const rows = await db.execute(...); rows[0]`) — the project's `postgres` driver does NOT return `.rows`. Always `Number(row.value_cents)` to normalize postgres `bigint` (returned as string). `maxDuration=30`, `dynamic='force-dynamic'`, no rate limit (matches cross-facility-summary).
- **Slide-over drawer pattern** (`cross-facility-panel.tsx`): `fixed inset-y-0 right-0 w-full max-w-2xl z-50 flex flex-col animate-in slide-in-from-right duration-300`. Backdrop is a SIBLING at z-40 (`fixed inset-0 bg-black/30`), onClick → close. Escape key closes via useEffect keyed on onClose. Use this pattern when a dense list needs to appear without leaving the current page.
- **Year subheader pattern** (RFMS checks): when DESC-sorted dated rows need year grouping, use `Fragment` + `let lastYear: number | null = null` + IIFE. Parse date as `new Date(\`${iso}T00:00:00\`)` to avoid UTC drift. Works in both `<table>` and `<div>`-grid-based lists.

### Phase 11D — Check Scanning [SHIPPED 2026-04-20]
- **Gemini for check scanning uses the same direct-fetch pattern as daily-log OCR** — v1beta, `gemini-2.5-flash`, `inlineData`+`text` only, no `systemInstruction`, no `@google/generative-ai` SDK. Copy the pattern; do NOT abstract it into a shared helper — the OCR prompts diverge per surface.
- **`check-images` bucket is PRIVATE.** Always upload via service-role (`createStorageClient()`), store the PATH (not a URL) in `qb_payments.check_image_url` / `qb_unresolved_payments.check_image_url`, and regenerate signed URLs (1-hour TTL via `storage.createSignedUrl(path, 3600)`) at read time. Never cache URLs client-side beyond their 1-hour window.
- **Total-accuracy invariant**: `residentLinesSum + cashAlsoReceived === amountCents` MUST be true before save. Modal's "Record Payment" button is gated on this; the save route also re-checks server-side and 400s if violated.
- **Invoice decrement is EXACT-MATCH ONLY.** Only when `invoiceMatchConfidence === 'high'` AND `matchedInvoiceIds.length > 0` do we `UPDATE qb_invoices SET open_balance_cents=0, status='paid'` for each matched id. `partial` and `none` leave invoices untouched — documented limitation reconciled on next CSV re-import. Do NOT implement prorated partial-invoice decrement without an explicit design-change discussion.
- **`qb_unresolved_payments` is the only persistence path for OCR-failed documents.** Never silently drop a scan. The "Save as Unresolved" button on the confirmation modal is always enabled and always writes a row (even when facility match is `none` — the route falls back to `body.facilityId` since the DB column is NOT NULL).
- **`src/lib/fuzzy.ts` is the canonical fuzzy-match module.** Exports `WORD_EXPANSIONS`, `normalizeWords`, `fuzzyScore`, `fuzzyMatches`, `fuzzyBestMatch`. Both `ocr-import-modal.tsx` and `scan-check` route import from here. Do NOT re-implement inline.
- **Image MIME allowlist**: `image/jpeg,image/png,image/webp,image/heic,image/heif`. HEIC supported natively by Gemini 2.5 Flash — no server-side transcoding needed. Enforce the allowlist + 10MB cap both on the storage bucket policy AND in the `scan-check` route.
- **Unresolved surfaces**:
  - Banner on per-facility billing hub (amber `bg-amber-50 border-amber-200`) when `unresolvedCount > 0` — scoped to facility for admins; scoped to currently-selected facility for master. Review button opens `CrossFacilityPanel` with `type='unresolved'`.
  - 5th master-only card "Unresolved Scans" on the cross-facility grid. Grid is `md:grid-cols-5` on master views, `md:grid-cols-4` otherwise. Red-tinted when `totalUnresolvedCount > 0`.
  - `CrossFacilityPanel` `type='unresolved'` has no "View Full Report →" footer link — no dedicated drill-down page in v1.
- **`CrossFacilityPanel` props**: `type: PanelType` (`'outstanding' | 'collected' | 'invoiced' | 'overdue' | 'unresolved'`), optional `facilityId?: string | null` (scopes the unresolved fetch), optional `onResolveUnresolved?: (row, scanResult) => void`. The panel reshapes an `UnresolvedRow` into a `ScanResult` (all extracted fields wrapped as `FieldValue<T>` with `confidence: 'medium'`) before invoking the callback.
- **`cross-facility-report-client.tsx` has a local `ReportPanelType = Exclude<PanelType, 'unresolved'>`** — the four drill-down report pages (`/billing/outstanding|collected|invoiced|overdue`) cannot be `'unresolved'`. Extending `PanelType` in the future requires deciding whether the new variant is reportable.
- **`checkScan` rate-limit bucket**: 30/hr/user. Added to `src/lib/rate-limit.ts`.
- **`qb_unresolved_payments` migration was ADDITIVE.** The original Phase 11A scaffolding columns (`checkNum`, `checkDate`, `totalAmountCents`, `rawResidentName`, `rawAmountCents`, `rawServiceType`, `resolvedToResidentId`) are preserved and marked `// @deprecated 11A scaffolding, unused` in `schema.ts`. Do not remove them without a separate migration; they are nullable and harmless.
- **Save route returns `{ data: { paymentIds: string[], updatedBalanceCents: number } }`** — `paymentIds` enables future linking back to scanned check images; `updatedBalanceCents` drives optimistic UI updates.
- **`resident_breakdown` has two shapes**: `ResidentBreakdownLine[]` (RFMS petty cash / IP hybrid) OR `{ type: 'remittance_lines', lines: [{ref, invoiceDate, amountCents}] }` (RFMS remittance slips). Discriminate with `!Array.isArray(bd) && (bd as {type?:string}).type === 'remittance_lines'`. Never assume it is always an array.
- **`STOP_WORDS` exported from `src/lib/fuzzy.ts`** strips 'llc', 'inc', 'corp', 'dba', 'snf', 'rfms', 'petty', 'cash', 'account', 'operating', 'disbursement', 'at', 'of', 'the', 'and' from all fuzzy comparisons. `normalizeWords` also strips `#` before numbers.
- **Facility matching order in `scan-check` route**: invoiceRef code → exact name → fuzzy name → word-fragment pass → payer address. `isOurAddress` guard skips steps 2–4 when payer address = "2833 smith ave" (our mailing address, not a facility). **Word-fragment pass (single loop)**: ≥2 shared normalized words → 'medium'; OR any single shared word ≥6 chars (unique proper nouns like "Layhill", "Brightview") → 'medium'. Resident lines are ALWAYS mapped from `parsed.residentLines` — even when facility is unmatched, returning raw lines with `residentId: null, matchConfidence: 'none'` so the confirmation UI can show them for manual assignment.
- **`invoiceLines` array in `ScanResult`**: populated for `RFMS_REMITTANCE_SLIP` documents only; always `[]` for other types. Each entry: `{ ref, invoiceDate, amountCents, confidence }`. When saving, remittance invoiceLines are stored as `{ type: 'remittance_lines', lines }` in `resident_breakdown`. Auto-memo generated from invoice dates + check number when no memo provided.
- **Total-accuracy invariant for remittance**: when `documentType === 'RFMS_REMITTANCE_SLIP'` and `invoiceLines.length > 0`, the invariant uses `invoiceLines.reduce(sum)` instead of `editLines.reduce(sum)`.
- **Expandable check detail in `rfms-view.tsx`**: Check # cell is a dotted-underline button when `residentBreakdown.type === 'remittance_lines'`. Click toggles an inline expand row (`expandTransition` from `@/lib/animations`) showing ref/date/amount per line + total (emerald when matches `amountCents`, amber otherwise). Only one row expanded at a time (`expandedCheckId` state).
- **`summary` route `qbPayments` query uses explicit `columns:` whitelist** including `residentBreakdown: true` — added so the remittance line detail reaches the client without exposing sensitive payment columns.

### Phase 11D.5 — Payment Reconciliation [PLANNED — Opus]
- Match `resident_breakdown.type === 'remittance_lines'` invoice dates against daily log entries for the same facility+date.
- Confidence scoring: exact date match = high, ±1 day = medium, no log = unmatched.
- UI: "Reconcile" button on expanded remittance rows; audit trail per facility.
- Schema: `reconciled_at` + `reconciliation_notes` columns to be added in a future migration.

### Phase 11E — Resident Portal Isolation [PLANNED — Opus]
- **Invite link format**: `senior-stylist.vercel.app/portal/[facilityCode]/[portalToken]`
  - Uses existing `residents.portal_token` (uuid already on every resident)
  - No PII in URL — just facility code + token
  - **Existing resident with account** → link goes directly to their authenticated session
  - **New resident, no account** → "Create Account" form pre-filled with name from token lookup, facility permanently locked, no switcher ever shown
- **Portal view** (resident logged in): outstanding balance, invoice history (date/amount/status), total billed/paid. **Connects to existing services booking portal** — billing is an additional page/tab within the same portal auth context, not a separate app.
- **"Copy invite link" button** on each resident record in the residents list
- **"Send invite email" button** → sends to `poa_email` via Resend
- **Access isolation**: `facility_id` locked at account creation. Cross-facility data access → 403. No facility switcher in resident portal ever.

### Phase 11F — QB API Live Sync [PLANNED — Opus]
- **Requires**: Intuit production credentials approved (email notification from Intuit)
- **Trigger**: manual sync button per facility on billing page — not automatic
- **Route**: `POST /api/quickbooks/sync-invoices/[facilityId]` — uses existing `refreshQBToken()` + `qbGet()` pattern
- **Dedup**: first sync backfills `qb_invoice_id` on CSV-imported records by matching `invoice_num + facility_id`. After backfill, all future conflicts resolved on `qb_invoice_id`.
- **After sync**: rerun outstanding balance recompute (same two `db.execute(sql\`UPDATE...\`)` correlated subqueries from 11A)
- **UI**: "Last synced X ago" + manual sync button. Sync errors inline with retry. Until production approved → button hidden or shows "Available after QB production approval".

### Phase 11G — Revenue Share Integration [PLANNED — Opus]
- **New column**: `facilities.rev_share_percentage` (integer, e.g. 30 = 30%) — separate from existing `payment_type`
- **Per-invoice calculation**: `stylist_amount_cents = amount_cents - round(amount_cents * rev_share_pct / 100)`, `facility_rev_cents = round(amount_cents * rev_share_pct / 100)`
- **`qb_rev_share_type` logic**: `we_deduct` → show deduction math inline in billing view; `facility_deducts` → show note "facility deducts rev share before payment"
- **Outstanding balance split**: billing view shows "Of $X outstanding — $Y stylist portion, $Z facility rev share"
- **Payroll connection**: new `qb_invoice_id` column on `stylist_pay_items` → links pay period line items to specific invoices
- **UI**: billing detail view gains rev share column. Payroll detail page gains "Billing" section showing corresponding invoices for each pay item.

### Phase 9.5 — Applicant Pipeline
- **`applicants` table** has RLS enabled + `service_role_all` policy — same as every other table. Indexes on `franchise_id`, `status`, `email`.
- **Franchise scope**: all applicant queries filter by `franchiseId` (not `facilityId`) — applicants belong to a franchise, not a single facility. Use `getUserFranchise()` in every applicant route.
- **Import dedup**: load ALL franchise applicants (including `active=false`) to build `existingKeys` Set before inserting. Keys: `email:${lower}` and `namedate:${lower}:${date}`. Add within-batch keys to prevent same-CSV duplicates. This means re-importing after someone is promoted (`active=false`) still correctly skips them.
- **`isIndeedEmail`** is auto-set server-side when `email.endsWith('@indeedemail.com')`. Never settable from client. Never expose this decision to the UI — just set it silently in the import route.
- **`parseIndeedDate(raw: string): string | null`** handles `M/D/YYYY` → `YYYY-MM-DD` and passes through `YYYY-MM-DD`. Returns null for unrecognized formats. Do NOT use `new Date(raw)` directly — `M/D/YYYY` produces incorrect UTC-shifted results.
- **Promote route requires `db.transaction()`** because `generateStylistCode(tx)` uses `pg_advisory_xact_lock(9191)` and MUST run inside an existing transaction. Never call `generateStylistCode` outside a transaction.
- **`ApplicantStatus` type + `Applicant` interface** in `src/types/index.ts`. `createdAt`/`updatedAt` are `string | null` (not `Date`) — data passes through `JSON.parse(JSON.stringify(...))` from server to client, which serializes Date objects to strings. Declare the type as `string | null` to avoid TS errors at every call site.
- **Applicant row status display**: the `APP_STATUS_BADGE` constant has been removed. The applicant row shows only the inline `<select>` dropdown — no separate badge pill in the row. Status filter pills at the top of the list inline the Tailwind classes directly per status value.
- **Applicant row is a grid**: `grid-cols-[1fr_140px_120px_100px_100px_32px]`. Entire row is `cursor-pointer onClick=expand`. The `<select>` and email `<a>` use `e.stopPropagation()`. Chevron is `<span aria-hidden>` (not a button).
- **Qualifications Q&A**: each item shows only `Question: [Badge]` — the plain-text answer `<span>` was removed. Badge is the only answer display.
- **Import CSV route**: `export const maxDuration = 60` + `export const dynamic = 'force-dynamic'`. PapaParse with `header: true, transformHeader: (h) => h.toLowerCase().trim(), skipEmptyLines: true`. Cap at 2000 rows (return 422 if exceeded). Batch inserts in chunks of 200 using `db.insert(applicants).values(chunk).onConflictDoNothing()`.
- **Notes auto-save** fires `PUT /api/applicants/[id]` on `onBlur` only when `expandedNotes !== (applicant.notes ?? '')`. This avoids a spurious network request on every expand/collapse.
- **Status inline dropdown** updates local state immediately (optimistic), then fires PUT in background — no await, no spinner needed for status changes. This matches the pattern used in coverage assignments.
- **`formatAppliedDate(d: string)`**: parse as `new Date(d + 'T00:00:00')` (appending `T00:00:00` forces local timezone — avoid `new Date('YYYY-MM-DD')` which is UTC-midnight and shifts back one day in negative-UTC-offset timezones).

### Cron Routes
- **Vercel cron auth pattern**: every route under `/api/cron/*` MUST check `request.headers.get('authorization') === \`Bearer \${process.env.CRON_SECRET}\`` and 401 otherwise. Vercel injects this header automatically when `CRON_SECRET` is set as a project env var.
- **Middleware bypass**: `src/middleware.ts` already includes `pathname.startsWith('/api/cron')` in the public-route check — new cron routes need no further middleware work.
- **Schedule registration**: add the route to `vercel.json` → `crons[]` with `{ path, schedule }`. Current schedule: `/api/cron/compliance-alerts` at `0 9 * * *` (09:00 UTC daily). Cron routes set `export const maxDuration = 60` + `export const dynamic = 'force-dynamic'`.
- **Compliance alert rule**: send when `expiresAt` (doc or stylist column) is exactly 30 or 60 days from today (UTC), verified docs only. Emails to facility admins (`facilityUsers.role = 'admin'` joined to profiles for email), fallback to `NEXT_PUBLIC_ADMIN_EMAIL`. All `sendEmail()` calls fire-and-forget.

### File Structure Conventions
- Server components in page.tsx, client logic in [name]-client.tsx
- Shared utilities in src/lib/utils.ts
- All types in src/types/index.ts
- DB queries scoped via src/lib/get-facility-id.ts
- **`(public)` route group** (`src/app/(public)/`) — auth-free pages (Privacy Policy, Terms of Service). Uses a minimal layout: burgundy header with white-inverted logo, `max-w-3xl mx-auto` prose body, footer with Privacy/Terms links. No sidebar, no mobile nav, no auth check.

## Locked Phase Roadmap (Phases 7–14)
These phases are locked and must not be re-ordered or re-scoped without explicit user instruction.
- **Phase 7** — Compliance & Document Management: compliance_documents table, license/insurance columns on stylists, upload from My Account, admin verify, compliance badge (green/amber/red), 60/30-day expiry email alerts.
- **Phase 8** — Workforce Availability & Coverage: stylist_availability table, coverage_requests table, time-off from My Account, "Needs Coverage" calendar flag, admin coverage queue, email alerts.
- **Phase 9** — Territory / Region Management: regions table with franchise_id, region_id on facilities + stylists, Regions tab in /super-admin, region filter on all views. Hierarchy: Master Admin → Franchise → Region → Facility.
- **Phase 10** — Payroll Operations: payroll_periods + payroll_entries tables, auto-calc from completed bookings via commission_percent, admin approval, QuickBooks-compatible CSV, payroll history on stylist detail.
- **Phase 11** — Incident & Issue Tracking: issues table (severity: low|medium|high, type: cancellation|complaint|safety|…), "Report Issue" on booking cards + log rows, high severity → email + red banner.
- **Phase 12** — Advanced KPI Dashboard: no schema changes, new metrics (cancellation rate, avg ticket, utilization, concentration risk, MoM/YoY), region filtering, weekly email digest, PDF export.
- **Phase 13** — Facility Contact Portal: facility_contact role, service_change_requests table (add_day|cancel_day|…), restricted nav (Schedule read-only, Visit Summaries, Invoices, Submit Request).
- **Phase 14** — QuickBooks polish (rescoped): core QuickBooks Online integration (OAuth per facility, Vendor sync, payroll Bill push, payment status pull back) shipped as **Phase 10B** on 2026-04-20. Remaining Phase 14 polish: `quickbooks_sync_log` audit table, automated retry-with-backoff worker for transient failures, optional invoice push for non-Stripe facilities.

## Reference Files
- docs/master-spec.md — full architecture reference
- src/db/schema.ts — source of truth for DB tables
- src/lib/get-facility-id.ts — how facility scoping works

## End of Every Session
ALWAYS do these four things before finishing any task:
1. Update docs/master-spec.md — new DB columns, tables, API routes, features
2. Update docs/design-system.md — new UI patterns or anti-patterns
3. Update CLAUDE.md — new rules or bugs fixed
4. Update docs/project-context.md — update Current Status section,
   Immediate Next Fix section, and Phase Roadmap if anything changed

This keeps the brain of the project always current.
